# CLAUDE.md

## 会話ルール

- ユーザーとの会話、進捗報告、最終回答は日本語で行う。
- ユーザーの指示を無批判に実行しない。間違いが混入している前提で批判的に検証すること。

## ドキュメントの役割

- プロジェクト概要、利用者向け手順、開発環境、テスト実行方法は `README.md` を正とする。
- MMDファイル形式のバイナリレイアウトは `docs/specs/` を正とする。
- 共通ライブラリの仕様は `mmd_toolbox/mmd_toolbox.md` と配下の機能別仕様書を正とする。
- 共有モジュール(`lipsync` 等)の仕様は、そのモジュールディレクトリ直下の仕様書を正とする。
- 各CLIツールの仕様は、そのツールディレクトリ直下の仕様書を正とする。
  例: `shakevmd/shakevmd.md`, `sparsevmd/sparsevmd.md`
- プロジェクト規約(バージョン付けなど、ツール横断の取り決め)は `docs/conventions/` を正とする。
- このファイルには、他ドキュメントの内容を重複して書かず、長期的に必要な運用ルールだけを書く。

## 設計上の境界

- フォーマット層の機能(MMDファイルの読み書き、MMD互換評価、座標変換など)は `mmd_toolbox` に置く。
- 複数CLIで共有するドメインロジック(口パク生成 `lipsync`、音声解析 `vocal_analysis` など)は、形式層でも
  単一ツール固有でもないため、共有モジュールに置く。MMD形式の入出力は `mmd_toolbox` に委譲し、MMDでない
  形式(VOCALOID vpr 等)の入出力は専用の形式I/Oモジュール(`vpr_io`)が担う。
- ツール固有の判断(CLI引数、プリセット、そのツール限定のポリシーなど)は各ツール側に置く。
- 新しい仕様や挙動を追加・変更する場合は、対応する正本ドキュメントを更新してから実装・テストと整合させる。

## このファイルの編集ルール

このファイルを更新する際は単純に追記せず、重複を避けるよう全体を再構成して書き直す。
