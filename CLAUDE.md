# CLAUDE.md

## 会話ルール

- ユーザーとの会話、進捗報告、最終回答は日本語で行う。
- ユーザーの指示を無批判に実行しない。間違いが混入している前提で批判的に検証すること。

## ドキュメントの役割

- プロジェクト概要、利用者向け手順、開発環境、テスト実行方法は `README.md` を正とする。
- MMDファイル形式のバイナリレイアウトは `docs/specs/` を正とする。
- フォーマット層の仕様は `libs/vmd/vmd.md`(と配下の機能別仕様書 `vmd-io.md`/`vmd-interp.md`/`vmd-camera.md`)・
  `libs/pmx/pmx.md`・`libs/vpr/vpr.md` を正とする。
- 共有モジュール(`lipsync` 等)の仕様は、`libs/` 配下のそのモジュールディレクトリ直下の仕様書を正とする。
- 各CLIツールの仕様は、`tools/` 配下のそのツールディレクトリ直下の仕様書を正とする。
  例: `tools/shakevmd/shakevmd.md`, `tools/sparsevmd/sparsevmd.md`
- プロジェクト規約(層タクソノミー、バージョン付けなど、ツール横断の取り決め)は `docs/conventions/` を正とする。
- このファイルには、他ドキュメントの内容を重複して書かず、長期的に必要な運用ルールだけを書く。

## 設計上の境界

層タクソノミー(フォーマット層/共有ドメイン層/CLIツール層)と物理配置・フォーマット層共通の設計原則の正本は
`docs/conventions/layering.md`。要点のみ:

- フォーマット層(`libs/` の `vmd`/`pmx`/`vpr`)が形式の入出力と形式に閉じた評価・変換を担う。MMD 形式の入出力は
  `vmd`・`pmx`、非 MMD 形式(VOCALOID vpr)の入出力は `vpr` が担う。
- 共有ドメイン(`libs/lipsync`・`libs/vocal_analysis`)は、形式層でも単一ツール固有でもないドメインロジックを担い、
  形式の入出力はフォーマット層へ委譲する。
- ツール固有の判断(CLI引数、プリセット、そのツール限定のポリシーなど)は各 `tools/` 側に置く。
- 新しい仕様や挙動を追加・変更する場合は、対応する正本ドキュメントを更新してから実装・テストと整合させる。

## このファイルの編集ルール

このファイルを更新する際は単純に追記せず、重複を避けるよう全体を再構成して書き直す。
