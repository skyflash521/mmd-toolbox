# song2vmd 要求仕様書

日本語の歌の音声ファイルから、あ・い・う・え・お・(ん)の標準表情モーフ(「ん」はオプション)によるリップモーションVMDを自動生成する独立CLIツールの要求仕様。

---

## 1. 位置づけ

### 1.1 結論

本機能は `song2vmd` という独立CLIツールとして提供する。

`song2vmd` は、日本語の歌の音声ファイル(BGM込み可)を入力に、ボーカル抽出・音素/母音認識・
声の強弱解析を経て、標準表情モーフによるリップモーションVMDを **1コマンドのフル自動**で出力する。

```text
入力 音声ファイル(歌, mp3/wav 等)
  -> song2vmd(ボーカル抽出 → 音素/母音認識 → 強弱 → モーフ生成)
  -> リップモーションVMD
  -> MMDで確認・編集
```

ボーカル抽出・音素/母音認識・強弱RMS算出の音声前段は共有モジュール
[vocal_analysis](../../libs/vocal_analysis/vocal_analysis.md) に委譲し(外部ツールは vocal_analysis が内部で呼び、固定
せず差し替えられる)、`song2vmd` はその共有出力から口形イベント列と開き量を作って `lipsync` へ渡す(4章・7章)。

リップモーションに必要なのは「時刻ごとの母音(口形)と強弱」であって歌詞の単語ではないため、song2vmd は歌詞
テキストの入力を要求せず、書き起こしテキストも出力しない。母音・音素を時刻付きで得る認識方式の選定と
品質測定は vocal_analysis の責務(vocal_analysis.md §5・§9)。

### 1.2 設計境界

[../../docs/conventions/layering.md](../../docs/conventions/layering.md) の設計境界に従う。

- フォーマット層(VMDの読み書き)は `vmd` に置く。
- 音声前段(ボーカル抽出・音素/母音認識・強弱RMS算出)と IPA→5母音写像規則・S-1認識測定は共有モジュール
  `vocal_analysis` に委譲する([vocal_analysis.md](../../libs/vocal_analysis/vocal_analysis.md)。7章)。
- アニメ的リップモーションのキーフレーム生成(標準口モーフの選択・母音合成プロファイル・生成方針)は共有モジュール
  `lipsync` に委譲する([lipsync.md](../../libs/lipsync/lipsync.md)。3章・6.5)。
- `song2vmd` 入口の判断(vocal_analysis の出力から口形イベント列を確定する処理＝IPA→母音写像の適用・両唇閉鎖
  判定・先頭子音種別(ConsonantClass)・開口減衰種別(ApertureClass)の判定・無音/閉口判定・境界補正、声の
  強弱→開き量の写像、CLI引数、プリセットの具体値)は `song2vmd` 側に置く。

### 1.3 直経路の採用(vpr を中間に挟まない)

`song2vmd` は音声から VMD への**直経路**を採り、vpr を中間に挟まない(音声→vpr→vmd と二段にしない)。

- 二段にすると、音声由来の**連続的な開き量(RMS)が音符のベロシティ/休符へ落ち、時刻も音符単位へ量子化される**
  ため、リップモーションの開き量と境界が鈍る。直経路はフル自動・音声→VMD の要件(1.1)とも整合する。
- 共有するのは**音声前段(vocal_analysis)とモーフ生成コア(lipsync)**であって vpr ではない。各CLIは2入力抽象
  (口形イベント列＋開き量)を別々の入口で用意して同じコアへ渡すので([lipsync.md](../../libs/lipsync/lipsync.md) §2)、
  コア再利用による効率と直経路による品質を両立できる。
- この劣化評価は設計上の判断であり、`vpr` の読み書きが整えば(vpr 経路を実際に通して)実測で確かめる。

### 1.4 独立ツールとする理由

- 既存ツール(`shakevmd`・`sparsevmd`・`mocapvmd`)はいずれもVMDを入力に取る。
  `song2vmd` は音声を入力に取り、共有モジュール(`vocal_analysis`・`lipsync`)を束ねる点で系統が異なる。
- 音声解析(分離・音素認識)はドメイン固有で、外部ツールへの依存と差し替えが本質。
  この依存管理を既存ツールへ混ぜない。

### 1.5 モーラの分割と開き量の単位

モーラの区間長が閾値以上のとき、そのモーラは複数の口形イベント(MouthEvent)に分割されうる(6.5)。
開き量は、分割されたモーラでは各口形イベントごとに個別の値を持つ。区間長・モーラ代表RMS(6.4の
二窓最大)・低信頼/無声判定・パーセンタイル母集団への算入は、分割の有無によらずモーラ全体に対して
1回だけ定まる値のままである(6.3・6.4・6.5)。この規定が本書全体の既定として働くため、開き量を単数で
述べる本書内の他の記述(2章・6.3・6.4・8章を含む)は、この規定が明示する区別(開き量は口形イベント
単位、それ以外はモーラ単位)に従う。

---

## 2. 目的

### 2.1 解きたい問題

MMDで歌にリップモーションを手付けするのは手間が大きい。自動生成したいが、フレーム単位で音声スペクトルを
追従して母音を推定する素朴な自動化は、次の問題を起こしやすい。

- モーフ値がフレームごとに細かく揺れ、口の動きが小刻みでせわしなく見える。
- フレームごとのスペクトル推定に任せて母音モーフが無制御に混ざり、口形が定まらず締まらない。
- 声の強弱をフレーム単位で連続追従するため、開き量が常に微振動する。

これらは、MMDの手付けやアニメで見られる「はっきり開閉し、1拍ごとに口形を保持する」リップモーションとは
かけ離れた見た目になる。なお、綺麗な口形のために複数の口モーフを組み合わせること自体は必要であり
有効である。避けるべきは「フレームごとの無制御な混在」であって、「設計された合成」ではない(3章)。

### 2.2 目標

- 日本語の歌の音声から、**アニメ的(MMD的)な、はっきり開閉するリップモーション**を自動生成する。
- 母音をモーラ単位で1つの主母音モーフとして保持し、フレーム単位の小刻みな揺れを避ける(3章)。
- 声の強弱を、各モーラの口の開き量(モーフ最大値)に反映する。
- ボーカル抽出・音素/母音認識の外部ツールを、あとで差し替えられる構成にする。
- 処理結果を数値レポートで確認できるようにする。

### 2.3 非目標

- 口モーフ(あ・い・う・え・お、任意で「ん」)以外の表情(まばたき、眉、目、感情表現)は生成しない。
  声から表情の感情までは判定しない。
- 日本語以外の言語は対象外とする。母音写像・特殊モーラ処理は日本語前提とする。
- 解剖学的な口形合成や、子音ごとの精密な口形再現は行わない。あくまで標準口モーフ(母音、任意で「ん」)
  によるリップモーション。
- 汎用キーフレーム疎化器は作らない。アニメ的な疎なキーは生成段階で直接置く(9章)。

---

## 3. アニメ的リップモーションの定義(品質基準)

本ツールが満たすべきリップモーションの質(アニメ的・MMD的な、はっきり開閉するリップモーション)の品質基準は、共有モジュール
`lipsync` の [lipsync.md §3](../../libs/lipsync/lipsync.md) を正本とする。設計された母音合成・開きすぎない・保持と
最小保持・協調調音・同母音連結・口形の先行準備・子音と無音の口形・疎なキーといった要件は
そちらで定義し、本書では重複定義しない。

`song2vmd` は、音声から確定した口形イベント列(6.3)と開き量(6.4・6.5)を `lipsync` に渡して、この品質基準を
満たすリップモーションを得る。母音の単位はフレームごとのスペクトルではなく、音素/母音認識で得た時刻付き音素(から
束ねたモーラ)に置く(4章)。

---

## 4. 処理パイプライン

音声前段(S0–S3: 入力読み込み・ボーカル分離・音素/母音認識・強弱RMS算出)は共有モジュール
[vocal_analysis](../../libs/vocal_analysis/vocal_analysis.md) に委譲する。`song2vmd` はその共有出力(ボーカルWAV・音素
セグメント列・RMS・IPA→5母音写像)から、入口処理で口形イベント列と開き量を作り、共有モジュール
[lipsync](../../libs/lipsync/lipsync.md) へ渡してリップモーションVMDを生成する。

```text
入力 音声ファイル(歌, BGM込み可)
  ├─[S0–S3] 音声前段        vocal_analysis(共有)→ ボーカルWAV・音素セグメント列・RMS・IPA→5母音写像
  ├─[入口]  口形イベント確定 song2vmd側 → 写像適用・両唇閉鎖判定・撥音判定・gap解決・無音/閉口確定・オンセット補正(6.3)
  ├─[入口]  開き量の決定     song2vmd側 → RMS→開き量(6.4・6.5)
  └─[S4]    モーフ生成       lipsync(共有)+ vmd → あ・い・う・え・お(既定。--n-morph 指定時は「ん」も)のリップモーションVMD
```

- すべての処理は `song2vmd` の1コマンド内で完結する。音声前段の外部ツール(分離・認識・復号)は
  `vocal_analysis` が内部で呼ぶ。利用者が外部ツールのコマンドを手で叩くことはない(vocal_analysis.md §8)。
- S0–S3 の責務(入力読み込み・ch/SR保持・入力レベル正規化・ボーカル分離・音素/母音認識・RMS算出・相対正規化)と
  正規化中間形式・IPA→5母音写像規則は **vocal_analysis.md §2〜§7 を正本**とする。`song2vmd` はその共有出力を
  消費する(6.1)。
- **口形イベント確定(`song2vmd` 入口)**: vocal_analysis の音素セグメント列に IPA→5母音写像(規則は
  vocal_analysis 提供)を適用し、両唇閉鎖判定・撥音判定・gap解決・無音/閉口の確定・母音境界のRMSオンセット補正を
  行って口形イベント列(母音・「ん」・両唇閉鎖・無音)を確定する(6.3)。
- **開き量の決定(`song2vmd` 入口)**: 各モーラのRMSから開き量を決める(6.4・6.5)。
- 確定した口形イベント列と開き量を `lipsync` へ渡し、3章のリップモーション(保持・閉口等)を生成する(6.5)。

---

## 5. コマンドライン仕様

### 5.1 書式

```text
song2vmd INPUT [options]
```

`INPUT` は音声ファイル(wav/mp3 等、外部ツールが扱える形式)。既定出力:

```text
<入力名>.vmd
```

### 5.2 オプション

| 引数 | 既定 | 説明 |
|---|---:|---|
| `INPUT` | 必須 | 入力音声ファイル |
| `-o, --output PATH` | `<入力名>.vmd` | 出力VMD |
| `--overwrite` | off | 出力先の既存ファイルへの上書きを許可する(上書きガードの解除。5.3) |
| `--model-name NAME` | `song2vmd <実行中のツールバージョン>`(例: `song2vmd 1.2.3`) | VMDに格納するモデル名(最大20バイト, Shift-JIS) |
| `--style NAME` | `pop` | 歌い方スタイルプリセット(8.1)。開き量レンジ・タイミングを切り替える |
| `--separate-vocals MODE` | `always` | ボーカル分離 `always` / `never` |
| `--separator NAME` | vocal_analysis の既定アダプタ | S1ボーカル分離バックエンドの選択(7章)。値・選択肢・既定は vocal_analysis の登録アダプタの安定 id に従う(vocal_analysis.md §8.2〜§8.3) |
| `--recognizer-model-id ID` | vocal_analysis の既定内容認識モデル(`ContentRecognizerModel.model_id`) | S2内容認識モデルの指定(vocal_analysis.md §5.2・§8.3)。未指定時は vocal_analysis の既定モデルを使う |
| `--recognizer-model-revision REV` | 既定モデルのリビジョン(`--recognizer-model-id` 未指定時)。`--recognizer-model-id` 指定時は未指定なら最新リビジョン | S2内容認識モデルのリビジョン指定(`ContentRecognizerModel.model_revision`)。`--recognizer-model-id` と組で使う。`--recognizer-model-id` を指定せず本引数だけを指定するのは対象が無く無意味なため引数エラー(11章・12.3) |
| `--no-recognizer-retry` | off(既定でリトライ有効) | S2内容認識のトリガ式リトライ(エコー幻覚・反復幻覚。主モデル自身をプロンプト無しで再認識する。vocal_analysis.md §5.2手順3)を無効にする。既定onの明示形 `--recognizer-retry` も受理する |
| `--forced-aligner NAME` | `wav2vec2-ctc-forcedalign` | S2強制アライメント段のバックエンド選択(vocal_analysis.md §5.3・§8.3)。値・選択肢は vocal_analysis の登録アダプタの安定id。既定のままなら以下の `--sofa-*` 系は一切不要で追加設定無しに現行どおり動く |
| `--sofa-python PATH` | 無し(必須) | SOFA専用venvのPython実行ファイルパス。`--forced-aligner sofa-forcedalign` 選択時のみ必須。既定(`wav2vec2-ctc-forcedalign`)時は指定しても未使用 |
| `--sofa-root PATH` | 無し(必須) | SOFAリポジトリのルートパス。`--forced-aligner sofa-forcedalign` 選択時のみ必須。既定時は指定しても未使用 |
| `--sofa-checkpoint PATH` | 無し(必須) | SOFAチェックポイント(`.ckpt`)ファイルパス。`--forced-aligner sofa-forcedalign` 選択時のみ必須。既定時は指定しても未使用 |
| `--sofa-timeout SEC` | `300` | SOFAサブプロセス1回あたりのタイムアウト秒数(正の数値のみ) |
| `--english-katakana-method NAME` | `arpakana` | S2 G2Pの英語カタカナ化フォールバック(vocal_analysis.md §5.2手順4)の変換方式選択。`arpakana`(ルールベース。生成モデル・GPU不要)または `tinyllama-katakana-converter`(生成モデル) |
| `--device MODE` | `auto` | 実行デバイスの選択(`auto` / `cpu`)。`auto` は環境から自動選択(GPU(CUDA)が利用可能ならGPU)。`cpu` はGPUを使わずCPUで実行する(音声前段の全モデルと、環境を継承するSOFAサブプロセスを含む)。GPUはあるがVRAMが不足する環境の回避手段 |
| `--n-morph` | off(既定は閉口) | 撥音(音節末の鼻音)に「ん」モーフ(`MouthShape.N`)を使う(既定off。6.3)。既定offの明示形 `--no-n-morph` も受理する |
| `--vowel-gain a:i:u:e:o` | `1:1:1:1:1` | 母音別(あ/い/う/え/お)の開き量微調整倍率。プリセットの母音別倍率(8.1)へ要素ごとに乗算する(既定はプリセット値そのまま。プリセット非依存の共通既定)。「ん」はプリセット値のままで本引数の対象外(8.2) |
| `--open-max V` | プリセット値(8.1) | 口の開き量の上限(開けすぎ防止) |
| `--coarticulation FRAMES` | プリセット値(8.1) | 隣接母音の協調調音の重なり長(上限) |
| `--anticipation FRAMES` | プリセット値(8.1) | 母音口形を音より先行させる最大フレーム数 |
| `--min-hold FRAMES` | プリセット値(8.1) | 最小保持フレーム。これより短いモーラは併合/間引き |
| `--intensity-curve P` | `0.6` | 強弱→開き量の非線形指数(累乗則。プリセット非依存の共通既定。8.1) |
| `--silence-threshold ON:OFF` | `0.06:0.10` | 無音(閉口)判定のヒステリシス開始/終了しきい値(プリセット非依存の共通既定。8.1) |
| `--max-duration SEC` | `300` | 長尺の自動分割境界(処理資源対策。0で無効。6.6) |
| `--dry-run` | off | 出力せず処理計画と診断を表示(6.7)。引数検証(上書きガード等)は dry-run でも実施する |
| `--keep-intermediate` | off | 中間生成物(正規化PCM・分離後ボーカルWAV・認識結果)を残す(診断用) |
| `-v, --verbose` | off | 通常実行でも `--dry-run` と同じ診断レポート(6.7)を標準出力へ表示する(出力VMDは書く) |
| `--quiet` | off | 進捗表示を抑制する(5.4)。警告・診断・終了コードは抑制しない |
| `--version` | — | バージョンを表示して終了する |
| `--machine` | off | 機械モード。標準出力を JSON Lines のイベントストリームにする(5.5・12章) |
| `--describe` | — | オプション定義とプリセット一覧を構造化して出力し終了する(5.5・12章) |

音声前段の外部ツールは vocal_analysis が内部で呼ぶ(7章)。モデル等の細かな呼び出し設定は vocal_analysis が
保持し(vocal_analysis.md §5.1・§8)、CLIにはバックエンドの「選択」・実行デバイスの指定(`--device`)と、
リップモーションの「効かせ方」のみを置く。
挙動パラメータの取り回しは[CLI インターフェース規約](../../docs/conventions/cli-interface.md) §9 に従う。

### 5.3 入出力要件

- 出力VMDの書き出しは `vmd.io` に委譲する。生成するのはモーフキーのみ。
- ボーン・カメラ・照明・セルフ影セクションは空で出力する。
- 文字列(モーフ名・モデル名)はShift-JIS(cp932)で格納する。
- フル自動のみとし、中間生成物を入力に取ってステージをスキップする運用は設けない。song2vmd 自身は、
  `--keep-intermediate` 未指定時、診断用の中間生成物を追加でディスクへ書き出さない(vocal_analysis が
  S1 で内部的に使う非公開の一時領域は vocal_analysis.md §4、および
  [一時ファイル・中間生成物の配置規約](../../docs/conventions/temporary-and-intermediate-files.md) §2.2
  の対象であり、本節の対象外)。指定時のみ、出力に隣接する `<出力ファイル名>.intermediate/` へ
  `input_normalized.wav`(S0正規化PCM)・`vocal.wav`(分離後ボーカルWAV。長尺分割時は重複区間を除いた
  各チャンクの担当区間(6.6)を連結した曲全体分。`--separate-vocals never` 指定時は分離を行わないため
  `input_normalized.wav` と同内容)・`segments.json`(S2認識結果)として保存する(5.2)。置き場所・命名・
  有効化契約の一般規約は同配置規約 §2.3 を正本とし、本機能はそこに従う。`--dry-run` と併用した場合も、
  最終VMDの書き出し(5.2)だけが抑制され、中間生成物の保存は実施する。
- **上書きガード**: 出力先の既存ファイル保護は[CLI インターフェース規約](../../docs/conventions/cli-interface.md)
  §6 に従う(終了コード2。11章)。
- **出力の原子性**: VMD は一時ファイルへ書き切ってから最終パスへ置換する(`vmd.io`)。途中終了で中途半端な
  出力ファイルを残さない(中断は12章)。
- **入出力の受け渡し・移植性**は規約 §9・§10 に従う(ファイルパス受け渡し・非ASCIIパス対応)。

### 5.4 進捗表示

工程は `load`(音声読み込み)・`separate`(ボーカル分離)・`recognize`(音素認識)・`rms`(音量解析)・
`events`(口形イベント確定)・`generate`(モーフ生成)・`write`(書き出し)の7段(12.1)。有効条件・
表記・停滞回避・副作用専用等の一般契約は[CLIインターフェース規約](../../docs/conventions/cli-interface.md)
§6が正。

`separate` 段(分離モデル)・`recognize` 段(内容認識モデル・音素モデル・
`--english-katakana-method tinyllama-katakana-converter`選択時はカタカナ生成モデルも)では、
それぞれのモデルの初回取得が実際にネットワークダウンロードを要した区間だけ、その進捗(取得済み割合)を
段の補足へ都度反映し、取得が終わり次第補足を消す(キャッシュ済みの取得では補足を出さない。12.1)。
両段はさらに、モデルのロード開始時(キャッシュ済みでダウンロードが発生しない場合を含む)にロード中で
ある旨を補足へ反映し、ロード完了までの区間を無補足にしない。加えて、モデル取得の有無に関わらず、
`separate` 段は分離処理(Demucs推論)の進捗、`recognize` 段は書き起こし・強制アライメントが処理中の
区間の時刻範囲を段の補足へ都度反映する(12.1)。

共有表示器(cli_progress)の構築時、標準エラーへの書き込みを `vocal_analysis.quiet.STDERR_WRITE_LOCK`
で排他する(pyopenjtalk のネイティブ拡張呼び出し中の fd 差し替えと競合しないようにするため)。

### 5.5 機械モードとメタ操作(機械可読インターフェース)

他のソフトウェアが子プロセスとして呼ぶ機械利用のために、機械可読の起動フラグを持つ。共通の契約
(チャネル固定・イベント種別の語彙・終端規則・終了コードの基底・標準出力の UTF-8 固定)は
[CLI インターフェース規約](../../docs/conventions/cli-interface.md)が正で、イベント送出は共有基盤
[cli_events](../../libs/cli_events/cli_events.md) を用いる。song2vmd 固有のイベントペイロード・`code` 値は
12章が定める。

- `--machine`: 出力を JSON Lines のイベントストリームにする(12章)。標準出力はイベント専用、標準エラーは
  人間向けログ・警告、終了コードは11章。既定(非指定)の人間向け表示・終了コードは変えない。
- `--machine --dry-run`: 入力検査。VMD を書かず、入力メタ情報と診断の result イベント(12章の
  `mode:"inspect"`)を出す。
- `--describe`: 自己記述。音声を読まずにオプション定義とプリセット一覧の result イベント(12章の
  `mode:"describe"`)を出して終了する。`--machine` を要さず単独で起動でき、入力 positional も要求しない
  独立メタ操作。
- `--help`/`--version` は `--machine` と併用しても人間向けテキストを出して終了コード0で終わり、イベント
  ストリームには載せない(処理を起動しないメタ操作のため。規約 §3 のメタ操作の例外)。`--help` は各
  オプション・引数の役割を人間向けに説明し、対話利用者が `--help` だけで使い方を把握できるようにする。
- 真偽フラグを対で持つ場合は `--x`/`--no-x` の様式に揃える(規約 §6)。

---

## 6. 機能要求

### 6.1 入力読み込み(S0)

入力読み込み(復号・入力レベル正規化・チャンネル数/サンプルレート保持)は共有モジュール
[vocal_analysis](../../libs/vocal_analysis/vocal_analysis.md) の S0(vocal_analysis.md §3)に委譲する。正規化の
実処理そのもの(復号・レベル正規化)には `song2vmd` は関与しない。ただし `song2vmd` はその S0 出力(正規化PCM)
をパイプライン内で保持し、長尺分割の境界決定・チャンク処理(6.6)、および `--keep-intermediate` の保存(5.3)
に用いる。

- **入力ゲインへの頑健性**: 入力の全体ゲイン差は vocal_analysis の入力レベル正規化(S0)で吸収され、分離・
  認識・RMS算出が入力音量に依存しない(同一音源のゲイン違いでリップモーション結果が変わらない。6.3・6.4)。

### 6.2 ボーカル抽出・音素認識・強弱RMS(S1–S3)

ボーカル抽出(S1)・音素/母音認識(S2)・強弱RMS算出(S3)は共有モジュール
[vocal_analysis](../../libs/vocal_analysis/vocal_analysis.md) に委譲する(vocal_analysis.md §4〜§6)。`song2vmd` は共有
出力(ボーカルWAV・音素セグメント列・RMS)を消費する。

- **ボーカル抽出(S1)**: vocal_analysis.md §4。分離の挙動 `always`/`never` の選択は `song2vmd` の CLI
  (`--separate-vocals`、5.2)で公開する。
- **音素/母音認識(S2)**: vocal_analysis.md §5。母音/子音/gap の全被覆セグメント列＋IPAラベル(任意の信頼度)を
  得る。内容認識モデルの選択は CLI(`--recognizer-model-id`・`--recognizer-model-revision`、5.2)で公開する。
- **強弱RMS算出(S3)**: vocal_analysis.md §6。相対正規化したRMSを得る。RMS を使う判断(無音/閉口・開き量)は
  `song2vmd` 入口(6.3・6.4)。

### 6.3 口形イベント列の確定(入口)

[vocal_analysis](../../libs/vocal_analysis/vocal_analysis.md) の音素セグメント列(母音/子音/gap＋IPAラベル)と RMS から、
`song2vmd` 入口で口形イベント列(母音・「ん」・両唇閉鎖・無音)を確定する。母音認識そのもの・IPA→5母音写像規則・
認識構成の採用と S-1 認識測定は vocal_analysis.md(§5・§7・§9)を正本とする。確定処理は次のとおり(確定した
口形イベント列からのリップモーション生成は lipsync。6.5):

- **あいうえお写像の適用**: vocal_analysis の IPA→5母音写像規則(vocal_analysis.md §7)を母音セグメントへ適用し、
  a→あ, i→い, u→う, e→え, o→お とする。
- **時刻の確定(オンセット補正)**: 認識のトークン境界を一次情報とし、近傍に S3 のRMS立ち上がり(オンセット)が
  あるときだけそれへ寄せて精緻化する。無い(レガートで同程度の音量が続く等)場合はトークン境界を用いる。
  補正窓は母音境界の前後 ±60ms(初期値)。オンセットは、平滑化(約30msの移動平均)した正規化RMSの上昇の
  傾きがしきい値(初期 +0.15/10ms)を超える最初の点とし、複数候補があれば元境界に時間的に最も近い点を選ぶ。
  窓幅・傾きしきい値は 8.3 の初期パラメータと同様に実データで調整する。
- **閉じ側の境界調整**: 認識構成の境界は母音の開始側が終端より相対的に正確で、終端(閉じ側=後続の
  子音・無音への切り替わり)は
  近似となる(vocal_analysis.md §9。開始側にも歌唱ではフレーズ内のずれがある。10章)。閉じ側のタイミングは
  上記のオンセット補正(母音開始側の判定が共有境界を動かすことで間接的に反映される)と、無音/閉口判定
  (6.4)の gap 走査だけで決まり、子音種別に応じた先行量調整や母音終端の打ち切り規則は持たない。息継ぎ
  (息の音が乗る短い区間)へ母音の開きが多少残ることは許容し、過剰に閉じ込まない側に倒す。
- **観測できる音響イベントで規則を書く**: 文字列が無いため、字面上の特殊モーラ(促音・語末など)を音響
  だけから一般には確定できない。撥音は採用構成(vocal_analysis.md §5.2)が専用の音素記号で頭子音の
  鼻音と区別して出力するため、その記号から確定する(下記)。よって口形イベントの確定は次の観測可能な
  イベントに基づく:
  - **連続した同一母音区間**: 同一母音が続き、かつ間に子音を挟まない(認識上の分割による観測上の連続で、
    実際には子音を挟まない)区間は、一つの口形イベントにまとめる。間に子音を挟む同一母音の連続モーラ
    (例:「たか」)は、間の子音が前後で異なる唇の方向・顎の開口減衰をもたらしうるため統合せず、それぞれ
    独立した口形イベントとして渡す(両唇閉鎖がま行等の頭子音で自動的に独立イベントになるのと同じ理由)。
    間の子音が前後で同じ音素であっても(例:「たた」)同じ規則で独立イベントとして渡す: 前後で唇の方向・
    顎の開口減衰の分類が同じでも、モーラの区切り(子音の再構音)そのものは実在するため、独立イベントとして
    渡すことで lipsync 側がこの境界にモーラ境界の谷(開口減衰の強さに応じた深さの一時的な閉じ)を入れて
    区別できるようにする(lipsync.md §4.2。谷はモーラの長さ・密集度によって間引かれたり、極端に短い
    区間では単一のピークへ平滑化されたりする。同§4.2・§4.9)。各モーラの開き量はその区間ごとの代表RMSから
    決まるため、開き量の違いがあればそこにも反映される。異なる母音は別の口形イベントとして渡す。統合の
    有無によらず、保持区間化・同母音連結・境界の協調調音は lipsync が行う(6.5)。
  - **撥音区間**: 撥音専用の音素記号(採用構成=G2P強制アライメントでは `ɴ`。vocal_analysis.md
    §5.2 の写像表で頭子音の鼻音(な行 `n`・にゃ行 `ɲ`・ま行 `m`/`mʲ` 等)とは記号が分かれているため、
    後続母音の有無を観測しなくても記号だけで一意に判定できる)を持つ子音セグメントを、既定では閉口
    にする。`--n-morph`(5.2)指定時のみ
    「ん」(`MouthShape.N`)にする。音節末の鼻音は口を閉じ気味に保つ有声の鼻音で、`--n-morph` 指定時は
    閉口や母音には倒さず「ん」モーフを正に立て(lipsync.md §2.1)、その開き量は区間の代表RMSから決める
    (閉じ気味なので通常は控えめ。6.4)。既定(`--n-morph` 未指定)の閉口は開き量を持たない(0)。
  - **閉鎖・無音区間**: 子音セグメントがま行・ば行・ぱ行の頭子音(採用構成の音素記号で `m`/`mʲ`・`b`/`bʲ`・
    `p`/`pʲ`。vocal_analysis.md §5.2)なら一瞬の閉口にする(撥音は専用記号 `ɴ` で上記のとおり
    区別されるため、ここで頭子音と撥音を振り分ける必要はない)。ɸ(ふ)は両唇音だが閉口しない(唇を
    丸める子音)ため閉鎖にしない。gap 区間は RMS で判定し、発声が続いている間は直前の母音的口形
    (母音・「ん」)を継続し、発声が終わった時点から gap の残りを無音(閉口)にする
    (gap 内の走査による分割。6.4。低ダイナミクス曲ではこの音量に基づく無音化を抑制し gap 全体を
    継続する)。先頭の gap・末尾の gap・直前の母音的口形が無い gap は、RMSに依らず閉口とする
    (口は閉口で始まり閉口で終わり、継続すべき口形が無い gap は開かない)。
    促音か無音かを字面で区別せず、観測した無音/閉鎖の長さで扱う。
  - **弱い/無声の母音区間**: 認識信頼度とRMSが低い母音区間は、開き量を弱めて `lipsync` に渡す(短すぎる区間は
    lipsync の最小保持で前後へ吸収される。6.5)。判定は、認識器が信頼度を出す場合は 信頼度 < 0.5 かつ
    モーラ代表RMS(6.4)< 0.3(初期値)、信頼度を出さないバックエンドでは モーラ代表RMS < 0.2(初期値)とし、
    該当区間の開き量を 0.5 倍(初期値)に弱める。しきい値・倍率は実データで調整する。
- **無音・閉口判定の責務**: 無音/閉口の確定は入口が、vocal_analysis のセグメント(母音/子音/gap)と S3 のRMS
  ヒステリシス(`--silence-threshold`、6.4)を併せて行う。gap の無音/継続は gap 内の走査による分割
  (6.4)で確定し、母音
  セグメントでも代表RMSが無音しきい値(下降側)以下の区間は、認識の信頼度によらず閉口にする
  (無声・誤検出対策)。ただし低ダイナミクス
  曲では、この音量に基づく無音化・無音補正を抑制する(音素由来の両唇閉鎖・先頭/末尾の閉口は対象外。6.4)。
- 両唇閉鎖(閉口)と撥音(音節末の鼻音)は独立した口形イベントにし、それ以外の子音は独立した口形イベントに
  しない(前後母音の協調調音は lipsync が扱う。6.5)。舌位置が主体の子音は、唇の方向は変えないが
  ApertureClass(開口減衰)により開口量を部分的に減衰させる。
- **先頭子音種別の付与**: 各母音イベントには、唇に影響する子音の口形変調のため、先頭子音種別
  (`lipsync` の ConsonantClass)を付与して渡す(判定・付与は入口の責務。lipsync.md §2.1)。母音セグメントに
  時間的に隣接して先行する子音セグメント(複数あれば母音に隣接する最後のもの)の IPA から判定する
  (採用構成=vocal_analysis.md §5.2 の音素記号で示す):
  ɸ・w(ふ・わ)は ROUNDED、ɕ・tɕ・dʑ・ɲ・ç(し・ち・じ・にゃ行・ひゃ行)および硬口蓋化(`ʲ` 付き。
  きゃ/ぎゃ/びゃ/ぴゃ/みゃ行等の拗音)の子音は SPREAD、その他の子音は NEUTRAL、先行する隣接子音が
  無ければ NONE。判定表に無い子音は NEUTRAL に倒す(唇を動かさない扱い)。
- **開口減衰種別の付与**: 各母音イベントには、顎の狭め具合による開口量の減衰(`lipsync` の ApertureClass)を、
  ConsonantClass とは独立に付与して渡す(判定・付与は入口の責務。ConsonantClass の判定とは別に動作し、
  一方の判定が他方の結果に影響しない)。直前の区切り(母音・gap・「ん」・両唇閉鎖のいずれか、または
  音素セグメント列の先頭)からその母音までの間に連続して現れる子音(両唇・撥音を除く)のうち、次の
  優先順(左ほど強い閉じ)で最も強い
  クラスをその母音の ApertureClass として付与する: FIRM_CLOSURE > NARROW_CHANNEL > SLIGHT_CLOSURE >
  NONE(区切りからその母音までに対象子音が現れなければ NONE)。両唇閉鎖は独立した完全閉口イベントであり、それより前の
  非両唇子音による開口減衰を閉鎖後の母音へ引き継ぐ理由はないため、区切りとして扱う。判定表(採用構成
  =vocal_analysis.md §5.2 の音素記号で示す):
  - `FIRM_CLOSURE`(舌先を歯茎へしっかり当てる): `t`, `d`, `n`, `ts`, `ɲ`(に行。硬口蓋鼻音だが「舌をしっかり
    当てる鼻音」として本クラスへ含める)。
  - `NARROW_CHANNEL`(狭めを作るが接触は無い/弱い): `s`, `z`, `ɕ`, `tɕ`, `dʑ`, `ç`, `j`。
  - `SLIGHT_CLOSURE`(接触はあるが顎への影響は弱い): `k`, `ɡ`, `ɾ`, `kʲ`, `ɡʲ`(拗音形の軟口蓋破裂音。破裂音
    としての閉鎖性は無標の `k`/`ɡ` と同じなので同じクラスへ含める。語尾 `ʲ` の一致では判定しない)。
  - `NONE`(残り): `ɸ`, `w`, `h`, `v`、および判定表に無い子音。
  各メンバー名・所属音は日本語の音韻論上の厳密な調音位置区分ではなく、開口減衰の強さでまとめた視覚
  チューニング用バケットである。ConsonantClass と ApertureClass は独立な軸のため、`ɕ`/`tɕ`/`dʑ`/`ç`/
  `ɲ`/`kʲ`/`ɡʲ` のように、一方が SPREAD でありながら他方が FIRM_CLOSURE/NARROW_CHANNEL/SLIGHT_CLOSURE
  に決まる(両軸が一致しない)組み合わせもある。「連続した同一母音区間」が1つの口形イベントへ統合されるのは、間に子音を
  挟まない場合(統合される後続モーラに先行子音が無い)だけなので、統合後の ApertureClass は先頭モーラの
  判定値(=区間全体の判定値。後続モーラに固有の子音は無い)を用いれば足り、情報は失われない
  (ConsonantClass も同じ扱い)。間に子音を挟む同一母音の連続モーラ(例:「たか」「たた」)は統合されず、
  各モーラが自分自身の ApertureClass を持つ独立イベントのまま lipsync へ渡る。
- あいうえお写像の適用・閉鎖判定・gap解決・観測イベント規則・口形イベント確定は日本語固有の入口処理として
  `song2vmd` 側に置く(vocal_analysis は写像規則と音素セグメント列・RMS を提供するまで)。
- **決定論**: 入口処理(あいうえお写像・両唇閉鎖判定・撥音判定・開口減衰種別の判定・gap解決・母音区間の
  無音補正・同一母音区間の連結・母音境界のRMSオンセット補正・低信頼/無声判定・先頭子音種別の付与)は、
  同じ入力(音素セグメント列・RMS)・同じCLIオプション/パラメータ(`--no-n-morph`・`--silence-threshold`
  等、6.3・6.4で挙げるもの)のもとで常に同じ口形イベント列を得る。判定が同値(RMS等)で割れる場合は
  連結(直前優先)側に倒す。

### 6.4 強弱(RMS)に基づく入口処理(S3の消費)

RMSエンベロープの算出と相対正規化は [vocal_analysis](../../libs/vocal_analysis/vocal_analysis.md) の S3
(vocal_analysis.md §6)に委譲する(入力ゲインに不変)。`song2vmd` 入口は、その相対正規化済みRMSを使って
無音/閉口判定と開き量決定を行う。

- **低ダイナミクス曲での閉口抑制**: 相対正規化は曲内に十分な強弱(無音と発声の差)がある前提のため、終始
  大音量で実質的な無音が無い曲では相対的に最も静かな部分を無音(閉口)と誤検出しうる。曲のダイナミックレンジ
  (発声部と最静部のレベル差。比なのでゲインに不変)が小さい曲では、**音量に基づく無音(閉口)判定**(発声が
  続く区間の無音化・母音区間の無音補正)を抑制する。音素に基づく両唇閉鎖や、先頭・末尾の構造的な閉口は抑制
  対象外。絶対レベルのしきい値は使わない(入力ゲインで結果が変わらないようにするため)。ダイナミックレンジは
  発声部と最静部の相対レベル差(95〜5パーセンタイルRMSのdB差。比なのでゲインに不変)で測り、下限
  (初期 12dB)未満なら抑制する。下限は実データで調整する。
- **モーラ代表RMS(中央60%・二窓の最大)**: 各モーラの代表RMSは、(a) 子音区間を除いた母音区間の
  中央 60%(初期値)の平均RMSと、(b) 吸収した先行子音区間を含むモーラ区間全体の中央 60% の平均RMS、
  の大きい方とする。(a) を基本に置くのは、子音トランジェントで開き量が乱れるのを防ぐため(母音区間は
  vocal_analysis の音素セグメントから得る)。(b) との最大を取るのは、強制アライメント
  (vocal_analysis.md §5.2)が伸ばして歌う発声の大部分を先行子音トークン側へ割り当て、母音区間を
  数十msまで狭めることがあり、その狭い母音区間のRMSだけでは発声中のモーラを代表できない(大声で
  歌っている最中のモーラが無音補正で閉口したり、開き量が過小になる)ため。この代表RMSを、開き量の
  決定(6.5)・母音区間の無音補正(6.3・本節ヒステリシス)・低信頼/無声の母音判定(6.3)のすべてに使う。
- **無音判定のヒステリシス**: gap の無音/継続および母音区間の無音補正は、開始(下降側)しきい値だけで
  判定する。終了(上昇側)しきい値は口形イベント確定のいずれの判定にも用いず、開始側との大小関係
  (`ON<OFF`)の引数検証、`--dry-run`/`-v` の診断レポート(6.7)への表示、`--describe`(12.2)の
  自己記述に現れるのみで、判定へは影響しない。
- **gap の走査による分割**: 直前が母音的口形の gap は、gap 全体をひとまとめに判定せず、正規化RMS
  エンベロープを先頭から走査して判定する。伸ばして歌う発声の尾部と真の無音が1つの gap に混在する
  ことがあり(強制アライメントは伸ばした母音の大部分を母音セグメントの外へ割り当てる。
  vocal_analysis.md §5.2 の既知の限界)、gap 全体の平均等による一発判定では継続・無音のどちらへ
  倒しても gap の一部に対して必ず誤るため。下降側しきい値以下が **0.2秒**(初期値)以上連続した
  最初の連続の開始時点、または gap 終端まで続く連続の開始時点で発声終了とみなし、そこで gap を
  分割して前半は直前の母音的口形の継続・残りは無音(閉口)とする。0.2秒の連続要件は、発声が同じ
  gap 内で続いている最中の瞬間的な谷(ビブラート・トレモロ)で早期閉口しないための余裕であり、
  gap 終端まで達した連続には適用しない(その先の発声再開が無く保護が不要なため)。一度閉じた gap
  内では再度開かない(無音を挟んで発声が戻る場合は直前母音の継続ではなく、新しい音素セグメントが
  置かれるべき領域のため)。しきい値以下の連続が無い gap は全体を継続する。値(連続要件 0.2秒)を
  変える場合は本書を先に更新する。
- **低信頼区間**: 認識・発声の信頼度が低い区間は、既定の最小開き量を保証せず閉口寄りに抑える。信頼度は
  認識器が出せる場合に用い、出せないバックエンドでは音量のみで代替する。
- 各モーラ区間のRMSから、そのモーラの口の開き量を決める(6.5)。

### 6.5 開き量の決定とモーフ生成への受け渡し

キーフレーム生成そのもの(3章の品質基準を満たすアニメ的リップモーション)は共有モジュール `lipsync` の
[lipsync.md §4](../../libs/lipsync/lipsync.md) のモーフ生成コアに委譲する。`song2vmd` は入口処理として開き量を決定し、
口形イベント列(6.3)・開き量・生成パラメータを `lipsync` に渡す。

- **モーラ代表RMSの声量レンジ再正規化(song2vmd 入口)**: 6.4のモーラ代表RMSは曲全体のフレームRMS基準
  ([vocal_analysis.md](../../libs/vocal_analysis/vocal_analysis.md) §6.1)で相対正規化された値であり、
  無音/発声の分離には有効だが、発声(母音・「ん」)区間だけを取り出すと正規化後の上端に偏りやすい。
  開き量の決定にだけ使う値として、曲全体の全モーラ(母音・「ん」)の代表RMS集合に対し、
  vocal_analysis.md §6.1と同じ方式(パーセンタイル線形正規化。初期値 `p10`→0・`p90`→1、範囲外は
  クリップ)で再正規化した値を用いる。無音/閉口判定(6.3・6.4のヒステリシス)・弱い/無声の母音判定
  (6.3)・低ダイナミクス抑制(6.4)は、この再正規化の対象にせず6.4の元の値(曲全体基準)をそのまま使う。
  長尺分割時は、結合後の曲全体のモーラ代表RMS集合に対して1回だけ再正規化を行う(6.6の曲全体基準の
  原則に従う)。分布に十分なばらつきが無い曲(縮退ケース。基準パーセンタイルの上下境界が一致する)
  では、全モーラを一律0.5に倒す(vocal_analysis.md §6.1の同種の縮退処理は無音判定用の正規化のため
  0.0に倒すが、本節は開き量という別用途のため、無音側でなく開閉の中間値0.5に倒す)。パーセンタイル比
  の相対計算であり入力ゲインに不変(vocal_analysis.md §6.1と同じ理由)。パーセンタイル範囲は実データで
  調整する。モーラが0件の曲(発声区間が検出されない)では本節の再正規化処理自体を行わない(開き量を
  決定する対象が無いため)。
- **開き量の決定(song2vmd 入口。RMS→開き量の写像)**: 各モーラ区間の再正規化後RMS(上記)を
  `--intensity-curve` の累乗則で写像し、`--open-max` とスタイルプリセットの開き量レンジ(強声/弱声の
  目安。8章)へクランプして、そのモーラの開き量(保持値スケール、0〜1相当)を決める。ただし、
  長時間モーラのサブウィンドウ分割(後述)では、この項目のクランプする写像ではなく、分割後の項目で
  定める連続的な写像を用いる。声が強いモーラ
  ほど大きく、ただし上限を超えて開かない。**このRMS→開き量の写像は `song2vmd` 側に残し、`lipsync` へは
  確定した開き量を渡す**(lipsync.md §2.2・§6)。`--intensity-curve` の指数と8.1の開き量レンジは、
  上記の再正規化後RMS分布(0〜1に広く分布)を前提に実データで調整する。
- **声量→開き量が機能している基準(実データ検証)**: モーラ代表RMSの再正規化とRMS→開き量の写像が
  実際に声量の強弱を開き量へ反映できているかは、代表サンプルで次の2基準の両方を満たすことで確認する。
  (a) 全モーラ(母音種別を区別せず一括)のモーラ代表RMSと生成された開き量のPearson相関係数が0.5以上
  であること。(b) 生成モーフキーのうち重み0.0(閉口・アタック/リリース端点)を除いた正のウェイト値を、
  出力された標準口モーフ名(あ/い/う/え/お/ん)ごとに集計し小数第2位に丸めて同値判定したとき、いずれの
  モーフ名についても出現頻度上位2値が占める割合が50%を下回ること(1つでも50%以上ならこの基準は不合格)。
  現行の `pop` プリセット既定値(8.1)はこの基準を満たすことを代表サンプルで確認済みである(他スタイルは
  8.1にあるとおり未チューニングの出発点値であり、この基準による確認はまだ行っていない)。基準を満たさない
  場合は、`--intensity-curve` の指数と8.1の開き量レンジの調整を優先し、それでも解消しない場合は
  `--open-max`(またはプリセットの`open_cap`初期値)の引き上げ、その次に該当モーフの母音別倍率の
  引き下げの順で対処する(母音別倍率は母音間の相対バランスを保つ役割を持つため、個別に下げる調整は
  最後の手段とする)。
- **lipsync へ渡す入力**: 6.3 で確定した口形イベント列(母音 a/i/u/e/o・「ん」・両唇閉鎖・無音/休符)、各母音
  イベントの先頭子音種別(ConsonantClass)・開口減衰種別(ApertureClass、いずれも6.3)、上記のモーラ別開き量、
  およびスタイルプリセット(8章)が与える生成パラメータ(アタック/リリース・協調調音・
  先行・最小保持・誇張など)。母音合成プロファイル・各生成パラメータの意味・キーフレーム生成方針
  (母音合成・保持・協調調音・先行準備・最小保持・疎キー配置・伸び表現)は lipsync.md を
  正本とし、本書では重複定義しない。
- **無音/閉口・両唇閉鎖の確定は入口で行う**: 無音・休符の区間や両唇閉鎖は、6.3・6.4 の観測イベント規則
  (RMSヒステリシス・両唇音判定)で口形イベントとして確定してから `lipsync` に渡す。`lipsync` 内では音量判定を
  しない。
- **長時間モーラのサブウィンドウ分割**: 歌唱で長く伸ばすモーラ(ロングトーン・メリスマ)は、その区間内で
  実際の声量が変化することがあり、代表RMSを1つだけ使う通常の決定では区間内の変化が開き量へ反映されない。
  この変化を追従させるため、区間長が長いモーラを複数のサブウィンドウへ分割し、サブウィンドウごとに開き量を
  決めて、同じ母音的口形を持つ、時間順・隙間なく連続する複数の口形イベントとして `lipsync` へ渡す(`lipsync`
  は既存の同母音連結(lipsync.md §4.2)によりこれらを1つの保持区間として連結し、各サブウィンドウの開き量を
  強弱節点として扱う)。
  - **対象と閾値**: モーラ(母音・「ん」)の区間長(終了時刻マイナス開始時刻、秒)が閾値(初期値1.0秒)
    以上のとき、そのモーラを複数のサブウィンドウへ分割する。閾値未満のモーラは、モーラ全体から求めた代表
    RMS1つに対応する開き量1つを持つ(分割しない)。
  - **サブウィンドウ数と境界**: サブウィンドウ数は、モーラの区間長を目標サブウィンドウ長(初期値0.3秒)で
    割った値を四捨五入(0.5は切り上げ)した整数とし、2未満なら2に引き上げる。モーラ区間をこの数で等時間幅に
    分割する(端数を残さず均等割り。目標サブウィンドウ長ちょうどにはならない)。
  - **サブウィンドウ代表RMS**: 各サブウィンドウ区間の中央60%窓平均RMS(6.4と同じ中央60%窓)とする。6.4の
    モーラ代表RMSが用いる「吸収した先行子音を含むモーラ区間全体窓との大きい方を取る」処理はサブウィンドウ
    には適用しない(この処理はモーラ先頭の子音吸収という文脈に依存するもので、モーラ内部のサブウィンドウ
    にはこの文脈が無いため)。
  - **正規化と写像**: 各サブウィンドウの代表RMSは、曲全体の全モーラ(母音的口形の各モーラにつき、
    分割の有無によらず1つずつの代表RMSからなる母集団。サブウィンドウの代表RMSはこの母集団に含めない)
    から求めたパーセンタイル境界(p10/p90)を使って個別に線形正規化する(0〜1、範囲外はクリップ)。
    この正規化後の値を`normalized`とし、`cap = min(開き量レンジの上限, --open-max)`・
    `floor = min(開き量レンジの下限, cap)`としたとき、サブウィンドウごとの開き量を
    `floor + (cap - floor) * (normalized の --intensity-curve 乗)`で決める(`normalized`へ先に
    べき乗を適用し、その結果を`floor`〜`cap`の範囲へ線形にスケール・平行移動する。この式自体は
    クランプを一切行わない)。`--open-max`が開き量レンジの下限を下回る場合は`floor = cap =
    --open-max`となり、`normalized`の値によらず開き量は常に`--open-max`の一定値になる。この写像は、
    分割されないモーラの開き量決定(べき乗則の後に開き量レンジ・`--open-max`へクランプする決定)とは
    異なる連続写像である。クランプする決定では、開き量レンジの外側に対応する広い正規化値域が同一の
    開き量(レンジの上限・下限)に潰れるため、1つの伸ばし音の区間内で複数のサブウィンドウの開き量が
    同一の一定値になり、区間内の声量変化が段階的な値の列に見える。サブウィンドウの開き量にはこの潰れを
    避けるため、上記の連続写像を用いる。
  - **低信頼/無声母音判定との関係**: 低信頼/無声の母音判定(6.3)は、分割前のモーラ全体の代表RMSと信頼度
    から一度だけ行う(サブウィンドウごとにやり直さない)。弱判定の場合は、全サブウィンドウの開き量へ一律に
    0.5倍のスケールを適用する。
  - **lipsyncへの受け渡し**: 分割後の各サブウィンドウは、元のモーラと同じ母音的口形を持つ、時間順・隙間
    なく連続する複数の口形イベントとして、lipsync.md §2の入力契約に従ってlipsyncへ渡す。先頭サブウィンドウ
    だけが元のユニットの先頭子音種別(ConsonantClass)・開口減衰種別(ApertureClass)を持ち、2番目以降の
    サブウィンドウは両方ともNONEとする(2番目以降には実在の先頭子音が無いため)。lipsyncは同母音連結
    (lipsync.md §4.2)によりこれらを1つの保持区間として連結し、各サブウィンドウの開き量を保持区間内の
    強弱節点として扱う。ApertureClassを先頭サブウィンドウだけに残すのは、lipsyncのモーラ境界の谷が、内部
    境界の直後のサブウィンドウのApertureClassが非NONEであることを谷生成の候補条件の一つとするため(この値を
    NONEにすれば当該境界は谷生成の候補にすらならず、実在しない谷が挿入される余地を確実に断てる)。
  - **パラメータの位置づけ**: 閾値(初期値1.0秒)・目標サブウィンドウ長(初期値0.3秒)はCLI引数として公開せず、
    song2vmd内の定数として持つ(6.5の再正規化パーセンタイル範囲と同じ位置づけ。実データで調整する)。
  - **診断出力の範囲**: `--dry-run`・`--machine`の診断出力(検出モーラ数・モーラごとの診断を除く)には、
    サブウィンドウ分割の有無・件数を示す項目を含めない。

### 6.6 長尺の分割

- 処理資源(メモリ・分離/認識の計算コスト)に配慮し、長尺音声を自動分割して処理する(`--max-duration`)。
  VMDのフレーム番号は十分大きい(u32)ため、分割はフレーム数上限対策ではなく資源対策である。
- 分割は歌唱の途中で切らないよう、無音区間で区切る。最初の目標境界は先頭から `--max-duration` 秒後、
  以降の目標境界は直前に確定した実境界(無音点または強制分割点)から `--max-duration` 秒後とする
  (固定グリッドではなく、実際に切った点を起点に積み上げる)。分割境界は、目標境界の前後 ±5秒(初期値)の
  探索窓で最も静かな点を選ぶ。探索窓内に無音が無い場合(ロングトーン・ライブ音源等)は最大チャンク長
  (`--max-duration`、5.2)で強制分割し、継ぎ目はオーバーラップで補う。探索窓幅は実データで調整する。
- 継ぎ目で品質を落とさないため、隣接チャンクは前後にオーバーラップ(初期 1.0秒)を持たせて分離・認識し、
  オーバーラップ区間はチャンク中央に近い側のセグメントを採用して重複を除く。境界をまたぐ連続母音区間は
  1区間へ結合し、キーの重複・欠落を避ける。継ぎ目の結合はセグメント段階で行い、結合後の全曲セグメント列に
  対してキー生成を1回行う(チャンク単位でキーを生成して貼り合わせない)。強弱の相対正規化(6.4)は曲全体を
  基準に行い、チャンク単位で基準がずれないようにする。オーバーラップ長は実データで調整する。

### 6.7 レポートと診断

`--dry-run` は以下を出力する。`-v`/`--verbose` は出力VMDを書く通常実行のまま、同じ内容を実行完了後に
標準出力へ追加で表示する(機械モードでは表示しない。12章)。

- 選択した各バックエンドと主要パラメータ。
- 分離の有無、認識音素数、検出モーラ数、音素認識の被覆率。
- モーラごとの口形(母音・「ん」)・保持値・保持長、併合/間引きしたモーラ数。
- 閉口区間、最大開き量、生成キー数。

**検出モーラ数・モーラごとの診断への影響**: 検出モーラ数およびモーラごとの口形・保持値・保持長は、
6.5のサブウィンドウ分割の有無によらず、実際に発声されたモーラの単位で数える。分割されたモーラは、
複数のサブウィンドウをまとめて1件のモーラとして数え、保持値にはそのモーラの全サブウィンドウの
開き量の平均、保持長には全サブウィンドウの保持長の合計を報告する。同じ理由により、6.5が定める
既存の受入基準(全モーラのモーラ代表RMSと生成された開き量のPearson相関係数が0.5以上であること)も、
分割されたモーラについては、そのモーラの全サブウィンドウの開き量の平均を、そのモーラの開き量として
用いる。

機械モードでは同じ診断を構造化して返す: 通常実行は result(`mode:"run"`)、`--machine --dry-run` は
入力メタ情報を併せた result(`mode:"inspect"`)に載せる(12章)。

### 6.8 資源逼迫の警告

処理中にVRAM・RAMの逼迫が実際に起きたことを観測した場合に警告を出す。判定にはモデル・曲・機材ごとの
需要見積りを使わず、観測された事実だけを使う。

- **GPUメモリの超過(`gpu_memory_oversubscribed`)**: GPU実行時、本プロセスのCUDAアロケータ要求量の
  最大値(処理開始からの高水位)が、処理開始時点の空きVRAMを超えたとき。この超過は算術的に確定する
  事実であり、超過分は物理VRAMに収まっていない(主記憶へ退避され処理が大幅に低速化する)。判定に
  しきい値定数は持たない。
- **スワップの検出(`swap_detected`)**: 処理開始からのスワップ(ページファイル)使用量の増分が
  ノイズ床を超え、かつ同じ区間で本プロセスツリーの使用量も増加しているとき(他プロセス起因の
  スワップで警告しないための帰属条件)。ノイズ床は他アプリの背景動作によるゆらぎを弾くためだけの
  定数で、判定結果を左右する見積り値ではない(初期値 64MiB。実データで調整する)。帰属条件の
  増加下限も同じ位置づけの定数とする(初期値 64MiB)。
- **判定タイミング**: 処理段の切り替わり時(12.1の progress と同じ stage 語彙の各段の開始時)。
  監視スレッドは持たない。要求量の最大値・スワップ増分はいずれも処理開始からの累積なので、段の
  途中で起きた逼迫も次の段の開始時に検出される。
- **1実行1回**: 同一の警告 code は1回の実行につき最初の成立時にだけ出す。
- **副作用専用**: 警告は出力VMD・終了コード・`--dry-run` の診断を変えない(5.4の進捗表示と同じ
  位置づけ)。人間向けには標準エラーへ `warning: <code>: <本文(観測値併記)>` の1行(6.4の
  低ダイナミクス警告と同じ形式)、機械モードでは warning イベント(12.1)を出す。
- **観測不能時の縮退**: 観測手段が使えない環境(GPUが無い・`--device cpu`・観測ライブラリの未導入・
  観測APIの失敗)では、該当する警告を黙って無効にする(エラーにせず、警告が出ないだけで処理へ
  影響しない)。スワップ使用量の増分が観測に現れない環境でも同様に、警告が出ないだけで他への影響は
  ない。

---

## 7. 外部ツール連携(vocal_analysis への委譲)

音声前段(S0–S3)の外部ツール連携機構——内部呼び出し方針(利用者にコマンドを叩かせない)、アダプタ
interface(Separator / Recognizer)、正規化中間形式、アダプタの登録と選択、採用ツールと代替候補、S-1 認識
測定——は共有モジュール
[vocal_analysis](../../libs/vocal_analysis/vocal_analysis.md)(§2・§8・§9)を正本とし、候補比較は
[vocal_analysis/external-tools.md](../../libs/vocal_analysis/external-tools.md) を補助資料とする。本書では重複定義しない。

- 利用者は `song2vmd INPUT` の1コマンドだけを実行する。音声前段の外部ツール(分離・認識・復号)は
  `vocal_analysis` が内部で呼び、利用者がそれらを手で実行することはない。
- バックエンドの選択(`--separator` / `--recognizer-model-id` / `--recognizer-model-revision` /
  `--no-recognizer-retry` /
  `--separate-vocals` / `--forced-aligner` / `--sofa-python` / `--sofa-root` / `--sofa-checkpoint` /
  `--sofa-timeout` / `--english-katakana-method`)は `song2vmd` の CLI で公開する(5.2)。アダプタの追加・切り替えや内容認識
  モデルの既定値・リトライ既定の変更は vocal_analysis 側で行い、`song2vmd` はその選択肢を引数として見せる。
- 音声前段の重い依存(分離・認識のライブラリやモデル取得)は `vocal_analysis` 側に閉じ、本リポジトリ本体の
  必須依存は `numpy/scipy` のまま保つ。

---

## 8. プリセットとパラメータ

リップモーションの効かせ方はプリセット(`--style`)で与え、実データでチューニングする。プリセットは「歌い方」に
よって口の開け方が変わることを反映する(8.1)。母音ごとの口形(母音合成プロファイル)は共有モジュール
`lipsync` が確定し、本書は参照する(8.2)。共通の調整パラメータは 8.3 に置く。

### 8.1 歌い方スタイルプリセット

歌い方(しっとり・元気・力強い・ささやき・早口など)によって、適切な口の開き量・遷移の速さ・誇張の
強さが変わる。これをプリセットとして用意し、主に**開き量レンジ**(弱声〜強声で取り得る開きの範囲)と
**タイミング**(アタック/リリース・最小保持・遷移・誇張)を切り替える。

| プリセット | 想定する歌い方 | 開き量レンジ(弱〜強, 目安) | タイミング・誇張の方針 |
|---|---|---:|---|
| `pop`(既定) | 標準的・はっきりした歌唱 | 0.30 〜 0.75 | 中庸。はっきり開閉し保持を効かせる |
| `ballad` | しっとり・落ち着いた歌唱 | 0.20 〜 0.55 | 開き控えめ。アタック/リリース長め |
| `powerful` | 力強い・ロック・シャウト | 0.40 〜 0.95 | 大きく開く。誇張強め、アタック速い |
| `whisper` | ささやき・静かな歌唱 | 0.10 〜 0.35 | 開き小さく閉じ気味。遷移も控えめ |
| `rap` | 早口・テンポの速い歌唱 | 0.30 〜 0.70 | 最小保持を短く、遷移を速く、きびきび |

開き量レンジは各モーラのRMS(強弱)を写像する下限・上限であり、`--open-max` で上限をさらに抑えられる。
プリセットは出発点で、`--open-max` などの個別オプション(8.3)で上書きできる。値は実データで調整する。

各プリセットの生成パラメータと、プリセットが与える CLI 既定の初期値(時間の単位はフレーム、30fps基準。
いずれも実データで調整する):

| 項目 | pop | ballad | powerful | whisper | rap |
|---|--:|--:|--:|--:|--:|
| アタック | 2 | 3 | 1 | 2 | 1 |
| リリース | 2 | 3 | 1 | 2 | 1 |
| 協調調音の重なり上限(`--coarticulation`) | 6 | 3 | 2 | 2 | 1 |
| 先行(`--anticipation`) | 11 | 1 | 2 | 1 | 1 |
| 最小保持(`--min-hold`) | 1 | 4 | 3 | 3 | 2 |
| 合成誇張係数 | 1.0 | 0.8 | 1.3 | 0.7 | 1.0 |
| 開き量上限(`--open-max`) | 0.90 | 0.70 | 0.97 | 0.50 | 0.85 |
| 母音別倍率 あ/い/う/え/お/ん | 1.30/1.20/1.70/0.80/1.70/1.00 | 全て 1.0 | 全て 1.0 | 全て 1.0 | 全て 1.0 |
| 三角形短区間の下限 | 2.0 | 2.0 | 2.0 | 2.0 | 2.0 |
| 伸び表現(しきい値/振幅/周期) | 10/0.05/22 | 18/0.05/15 | 18/0.05/15 | 18/0.05/15 | 18/0.05/15 |
| レガート谷(浅/深/傾き) | 0.45/0.30/0.02 | 0.4/0.2/0.025 | 0.4/0.2/0.025 | 0.4/0.2/0.025 | 0.4/0.2/0.025 |
| モーラ境界の谷(半幅/最低間隔) | 12.0/4.0 | 12.0/4.0 | 12.0/4.0 | 12.0/4.0 | 12.0/4.0 |

`pop` の母音別倍率・先行・協調調音・伸び表現・レガート谷は、同じ `lipsync`
生成コアを使うリップモーション生成系のMMD視覚チューニングで確定した標準値。他スタイルは未チューニングの
出発点値で、必要になったら個別に視覚で詰める。プリセットは母音別倍率を含む上記の生成パラメータの
大半を明示的に持つが、モーラ境界の谷(半幅/最低間隔)だけは全プリセットが `lipsync` の生成既定
(lipsync.md §4.8)をそのまま使い、値を個別には持たない。

`pop` の最小保持(`--min-hold`。単位は本節冒頭のとおり30fpsフレーム)は、他スタイルおよび移植元の
リップモーション生成系とは異なる値(1)を持つ。S2の最小滞在制約(vocal_analysis.md §5.2。単位は20msフレーム)
により song2vmd のモーラ長は概ね最小滞在に相当する **120ms(30fps換算で約3.6フレーム)** 付近に
集まり、`lipsync` の三角形短区間分類(30fpsフレームで「区間長 − 最小保持 < 2」なら三角形扱い。
lipsync.md §4.4)の閾値に接近しやすい。最小保持がこの移植元の値(3)のままだと 3.6 − 3 = 0.6 < 2 で
無音直後の母音(先行準備の対象)を含む多くのモーラが三角形扱いになり、三角形は先行準備の対象外
(lipsync.md §4.9・§4.10)のため先行準備(`--anticipation`)が実効上まったく発動しない実測結果に
なった。1へ調整すると 3.6 − 1 = 2.6 ≥ 2 で三角形扱いを避けやすくなり、song2vmd の実際のモーラ長
分布に合う(実データで再調整する)。

プリセットに依らない共通の初期値: 強弱→開き量の累乗指数(`--intensity-curve`)は 0.6、母音別の開き量
微調整倍率(`--vowel-gain`。プリセットの母音別倍率へ乗算する。8.2)は 1:1:1:1:1、無音ヒステリシス
(`--silence-threshold`)は 開始(下降側)0.06・終了(上昇側)
0.10、両唇閉鎖は深さ=全母音0・長さ=認識構成が返す子音区間長そのまま(上限クランプは持たない)。

### 8.2 母音口形(母音合成プロファイルは lipsync が確定)

母音ごとの口形を標準口モーフの重みへ合成する母音合成プロファイルは、共有モジュール `lipsync` が
ドメイン規約として確定する。正本は [lipsync.md §4.1](../../libs/lipsync/lipsync.md) であり、本書は参照のみで
定義を複製しない(主: `lipsync` / 従: 本書)。`song2vmd` はプロファイルを定義・上書きせず、6.3 で確定した
口形イベント列と開き量・生成パラメータ(誇張係数など)を `lipsync` へ渡す(6.5)。`song2vmd` 側で母音の
口形に関与するのは母音別の開き量倍率(プリセットの母音別倍率と `--vowel-gain`)と開き量レンジ・上限
(8.1・8.3)であって、母音→モーフ重みの合成規則そのものではない。`lipsync` の `vowel_scale` は
母音的口形別(a, i, u, e, o, ん)の6要素倍率(lipsync.md §4.8)なので、`song2vmd` は**プリセットの
母音別倍率(6要素。8.1)の a〜o の5要素へ `--vowel-gain` の5母音値を要素ごとに乗算**し、「ん」は
プリセット値のまま `(a, i, u, e, o, ん)` の6要素として渡す。`--vowel-gain` はプリセットのチューニング済み
バランスからの相対微調整であり(既定 1:1:1:1:1 はプリセット値そのまま)、撥音の開き量は `--vowel-gain`
で個別調整しない。`--n-morph` 指定時、「ん」モーフの開き量はプリセットの「ん」倍率とその区間のRMS・
「ん」プロファイルで決まる(6.3・6.4)。既定(`--n-morph` 未指定)の閉口は開き量を持たない(0)。

### 8.3 共通パラメータ

リップモーションの効かせ方を決める初期パラメータ。具体値(プリセット別の値と共通既定)は 8.1 に定め、実データで
調整する:

- 開き量レンジ(弱声・強声の目安)と上限(`--open-max`)。口を開けすぎない上限の主役。
- 強弱→開き量の非線形指数(`--intensity-curve`)。弱い声を持ち上げすぎない累乗則。
- 最小保持フレーム(`--min-hold`)。小刻み抑制の主役。
- アタック/リリースのフレーム数。
- 協調調音の重なり長(`--coarticulation`)と母音口形の先行フレーム数(`--anticipation`)。
- 母音別の開き量倍率(プリセットの母音別倍率 × `--vowel-gain` の微調整。8.1・8.2)。
- 無音判定のヒステリシス開始(下降側)しきい値(`--silence-threshold` の ON 側)、両唇音の閉口の深さ。

長い伸ばし音の揺らぎ(伸び表現)・レガート谷・三角形短区間の下限は各プリセットが具体値を持ち(8.1)、
モーラ境界の谷(半幅・最低間隔)は全プリセットが `lipsync` の生成既定を使う(8.1)。いずれも CLI
からは上書きしない。

初期値(8.1)は、歌唱内容・ミックス・モデルにより適切値が変わる前提で、レポート(6.7)と
MMD上の視覚確認で調整する。特に開き量レンジ・最小保持・アタック/リリース・強弱指数は、
小刻み感・開きすぎ・口の生き生きさのトレードオフに直結するため重点的に調整する。

---

## 9. vmd との関係

- VMDの読み書きは `vmd.io` に委譲する。`song2vmd` はVMDのバイナリ構造を直接扱わない。
- 生成するのはモーフキーのみ。モーフキーがキー間で線形評価される事実(補間曲線を持たない)は
  [vmd-interp.md](../../libs/vmd/vmd-interp.md) を正本とする。
- アニメ的な疎なキーは生成段階で直接置く(キーフレーム生成は 6.5 で共有モジュール `lipsync` に委譲)。
  ボーン/カメラ用のベジェ疎化のような汎用疎化器は作らず、`vmd` にモーフ用の疎化機能も追加しない
  (設計境界どおり、アニメ的リップモーションの生成判断は共有モジュール `lipsync` 側、フォーマット入出力は
  `vmd` 側)。
- 既知の制約: 既存の `vmd.reduce` はボーン/カメラ専用(`Tolerances` も `bone_*`/`camera_*` のみ)で、
  モーフ(スカラー)キーの疎化には対応していない。song2vmd は汎用疎化を使わず生成段階で直接疎キーを置くため、
  この非対応は本ツールの障害にならない。将来モーフ疎化を共通機能として持たせる場合は、`vmd` 側に
  モーフ対応の追加(修正)が必要になる。
- 時間軸は30fps基準(`vmd` 規約)に従う。

---

## 10. 制約と考慮事項

- 日本語の歌のみを対象とする。母音認識の誤り(歌唱の崩れ・ロングトーン・無声化・コーラス混入など)は
  そのまま口形に出る。
- 母音認識は歌唱で誤ることがある。認識方式・認識構成の選定と品質測定は vocal_analysis が正本
  (vocal_analysis.md §5・§9)。認識の時刻は開始側が母音の終端(閉じ側)より相対的に正確で、終端は
  近似のため、境界は入口の
  RMSオンセット補正・閉じ側調整で補正する(6.3・6.4)。ただし開始側にも歌唱ではフレーズ内の
  ずれ(次項)がある。将来、任意の歌詞テキスト指定による精度向上の余地を残す。
- **フレーズ内のモーラ時刻ずれへの入口側補正は実測で棄却済み(再試行しない)**: 歌唱では認識の
  フレーズ内時刻が実発声から数百msずれることがある(音響証拠の欠如による。vocal_analysis.md §5.2 の
  既知の限界「歌唱でのフレーズ内時刻ずれ」)。これを入口側で補正する次の2方式は、人手作成の
  リップシンクVMDを参照とした母音一致の開発時比較(vocal_analysis の S-1 測定とは別)の実測で
  効果が無い(悪化する)ことを確認済み: (a) RMSエンベロープの谷(子音の閉鎖・息継ぎ)へモーラ
  境界を単調マッチングして時間ワープする方式は、系統的に遅れた時刻が近傍の遅い側の谷へ吸着するだけで
  一致率を悪化させる。(b) 口形イベント終端後のRMS持続に応じて保持を動的に延長する方式は、延長の
  不足分がRMS持続と正の関係を持たず(実測ではむしろ負の相関)、延長量をRMS持続から決める単純規則の
  適用は人手保持との残差をかえって増やした。そもそも本ツールの出力の保持は人手作成の
  リップシンクより長い(gap継続・最小滞在・子音吸収が既に伸長として働いており、「伸ばし不足」という
  前提自体が成立しない)。時刻ずれの根治は vocal_analysis 側の音響モデル/アライナー差し替えで行う。
- 処理資源に配慮し、長尺は自動分割する(6.6。VMDフレーム番号は u32 で十分大きく上限対策ではない)。
- 音声前段の入力読み込みの方式(対応形式・復号のフォールバック・復号器を再配布しない方針・利用者にコマンドを
  叩かせない方針)は vocal_analysis.md §3・§8 を正本とする(7章)。
- 外部ツール・モデルの取得失敗や実行失敗の場合は、どのステージで失敗したかを明示して停止する
  (終了コード4・`stage_failed`。11章・12.3)。
- 将来拡張(初期非対象): 感情・表情モーフの付与、英語等の多言語対応、歌詞テキスト指定による高精度化。

---

## 11. 終了コード

| コード | 意味 |
|---|---|
| 0 | 正常終了 |
| 1 | 入力不正(存在しない・音声として読めない・破損・非対応形式) |
| 2 | 引数エラー(範囲・書式・上書きガード・未知オプション など) |
| 3 | 出力書き込み失敗 |
| 4 | 音声前段の外部依存の失敗(復号器未検出・分離/認識のモデル取得・実行失敗。失敗ステージを明示する。10章) |
| 130 | 協調的な中断(Ctrl-C 等。12.4) |

- `0`〜`3` は[CLI インターフェース規約](../../docs/conventions/cli-interface.md) §5 の基底と同じ意味。
  `4` は song2vmd 固有の追加コード、`130` は全ツール共通の中断予約コードで、いずれも基底 `0`〜`3` の意味へ
  押し込めない。
- 想定外の内部エラーは最も近い基底へ寄せて `1` で終える。機械モードでは `internal_error` の error イベント
  (12.3)で入力起因の `not_audio` と識別でき、非機械モードでは理由を標準エラーへ最低1行出す(いずれも
  トレースバックは出さない)。
- 失敗は理由を必ず表に出す: 機械モードでは終了コードに加えて error イベントで「どのフィールド/パスが・なぜ
  不正か」を返し(12.3)、非機械モードでも失敗理由を標準エラーへ最低1行出す(終了コードだけで返す経路は
  持たない)。

---

## 12. 機械モード(機械可読インターフェース)

機械利用(子プロセス呼び出し)向けに、`--machine` で出力を JSON Lines のイベントストリームへ切り替える
(5.5)。共通の契約・語彙は[CLI インターフェース規約](../../docs/conventions/cli-interface.md)が正、
イベント送出は共有基盤 [cli_events](../../libs/cli_events/cli_events.md) を用いる。本章は **song2vmd 固有の
ペイロード形・`code` 値**を定める(規約は種別の語彙と終端規則のみを共通化し、ツール固有ペイロードは持たない)。

- **チャネル固定・符号化**(規約 §3・§10): 機械モードの標準出力は §12.1 のイベント**のみ**。人間向けログ・
  警告テキストは標準エラーへ出す。
- **終端規則**(規約 §4): ストリームは result または error の**ちょうど1つ**で終端する(強制終了時のみ
  例外。12.4)。
- 各イベントは種別フィールド `type` を持つ。`code` は機械利用側の分岐に使い、人間向け `message` と分離する。
  song2vmd 由来の `code` は安定 snake_case とする。
- **イベント契約の進化**は規約 §4.1 に従う(フィールド・種別・`code` の追加=MINOR、削除・意味変更=MAJOR。
  受信側は未知要素を無視できる前提)。実装で増える警告・統計は本章へ追記して追加する。

### 12.1 イベントペイロード

各イベントは `type` を持つ。

- **progress**: `{type:"progress", stage, done, total, note, elapsed}`。`stage` は処理段階の安定 id:
  `"load"`(音声読み込み。S0)/ `"separate"`(ボーカル分離。S1)/ `"recognize"`(音素認識。S2)/
  `"rms"`(音量解析。S3)/ `"events"`(口形イベント確定)/ `"generate"`(モーフ生成。S4)/
  `"write"`(書き出し)。
  `done`/`total` は長尺分割(6.6)時の処理済み/総チャンク数(分割しない場合や内訳の無い段は
  `done=0, total=null`)、`note` は補足文字列(無ければ `""`)、`elapsed` は段開始からの経過秒。
  各段は開始時に最低1本の progress を出す。`separate` 段(分離モデル)・`recognize` 段(内容認識
  モデル・音素モデル・`--english-katakana-method tinyllama-katakana-converter`選択時はカタカナ
  生成モデルも)は、そのモデルの初回取得が実際にダウンロードを要した区間だけ、`done`/`total`
  (長尺分割時はそのチャンクの値のまま)を保った追加の progress を都度出し、`note` へ取得済み割合を
  反映する。取得が終わり次第 `note` を空文字列に戻す1本を出す(キャッシュ済みの取得ではこれらの
  追加 progress 自体を出さない)。両段はさらに、モデルのロード開始時(キャッシュ済みで
  ダウンロードが発生しない場合を含む)にロード中である旨(`separate` 段は`f"モデル読み込み中:
  {ファイル名}"`、`recognize` 段はモデル種別ごとに`f"内容認識モデル読み込み中: {model_id}"`・
  `f"音素モデル読み込み中: {model_id}"`・`f"カタカナ生成モデル読み込み中: {model_id}"`)を
  `note` へ反映する1本を出し、ロード完了までの区間を無補足にしない。加えて、モデル取得の有無に
  関わらず、`separate` 段は分離処理
  (Demucs推論)が実際に進行している区間、`recognize` 段は書き起こし・強制アライメントの各区間処理が
  進行している区間でも追加の progress を都度出し、`note` へ処理内容を反映する
  (`separate` 段は`f"分離中: {percent}%"`、`recognize` 段は書き起こし中`f"歌詞書き起こし中:
  {開始}-{終了}({区間番号}/{区間数})"`・アライメント中`f"音素アライメント中:
  {開始}-{終了}({区間番号}/{区間数})"`。時刻は`m:ss`形式)。
- **warning**: `{type:"warning", code, message}`。仕様が定める警告は `low_dynamics_suppressed`
  (低ダイナミクス曲で音量に基づく無音化を抑制した。6.4)と `forced_split`(無音が見つからず最大チャンク長で
  強制分割した。6.6)、および資源逼迫の2警告(6.8。いずれも `stage`(検出した段。progress と同じ
  安定 id)と観測値のキーを追加で持つ):
  - `gpu_memory_oversubscribed`: 追加キー `reserved_mib`(CUDAアロケータ要求量の最大値)・
    `free_at_start_mib`(処理開始時点の空きVRAM)・`total_mib`(VRAM総量)。いずれもMiBの整数。
  - `swap_detected`: 追加キー `swap_increase_mib`(処理開始からのスワップ使用量の増分)・
    `process_rss_mib`(検出時点の本プロセスツリーの使用量)・`ram_total_mib`(搭載RAM量)。
    いずれもMiBの整数。

  その他の警告は実装で本章へ追記して増やす(追加は後方互換。規約 §4.1)。
- **result**: 正常終了の終端イベント。`mode` で形が決まる:
  - `mode:"run"`(通常実行): `{type:"result", mode:"run", output, keys, backends, style, separated,
    phonemes, morae, merged_morae, coverage, closed_ranges, max_opening, duration_sec}`。
    `output` は書き出しパス(文字列)、`keys` は出力 VMD のモーフキー数、`backends` は採用バックエンド
    (`{separator, recognizer, forced_aligner, english_katakana_method}`)、`style` はプリセット名、
    `separated` は分離を実施したか(bool)、
    `phonemes` は認識音素数、`morae` は検出モーラ数、`merged_morae` は併合/間引きしたモーラ数、
    `coverage` は音素認識の被覆率(0〜1)、`closed_ranges` は閉口区間数、`max_opening` は最大開き量(0〜1)、
    `duration_sec` は入力音声の尺(秒)。6.7 の診断の構造化。
  - `mode:"inspect"`(入力検査 `--machine --dry-run`): `mode:"run"` と同じ診断統計キーに加えて
    `input_kind, sample_rate, channels` を持ち、VMD を書かないので `output` は `null`、`keys` は生成予定の
    モーフキー数。`input_kind` は常に `"audio"`、`sample_rate`/`channels` は入力音声のメタ情報。
  - `mode:"describe"`(自己記述 `--describe`): `{type:"result", mode:"describe", options, presets}`(12.2)。
    処理統計のキーは載せない。
- **error**: `{type:"error", code, exit_code, field, path, message}`。`field`/`path` は対象が無ければ `null`。
  `stage_failed` のみ、このキーに加えて失敗ステージを示す `stage` キーを持つ(12.3)。失敗の終端イベント。
  `code`/`field`/`exit_code` の割り当ては 12.3。

### 12.2 `--describe` の中身

`options` は**処理を駆動する引数**の配列(5.2 の表のうち、メタ/モード操作 `--describe`/`--version`/
`--help`/`--machine` を除く全引数)。各要素は `{name, type, constraint, default, help}`(キーは常に5つ、
該当しない値は `null`):

- `name`: 長形式フラグ文字列(例 `"--style"`)。positional は `"input"`。
- `type`: 固定語彙 `"float"` / `"int"` / `"str"` / `"flag"`(真偽)/ `"enum"`(選択肢)/ `"compound"`
  (複合トークン)。
- `constraint`: 数値(`float`/`int`)は `{min, max, exclusive_min}`、`enum` は `{choices:[...]}`
  (`--style` は 8.1 のプリセット名、`--separate-vocals` は `always`/`never`、`--separator` は
  vocal_analysis の登録アダプタの安定 id)、`compound`
  (`--vowel-gain`=`"a:i:u:e:o"`・`--silence-threshold`=`"ON:OFF"`)は `{format, fields}`(`format` は
  トークン文法の文字列、`fields` は各トークンの数値制約の配列)、`flag` と制約なしは `null`。制約値は CLI
  実装の引数検証から機械導出し、手書きで複製しない。
- `default`: 解決後の既定値。プリセット由来の既定(`--open-max`/`--coarticulation`/`--anticipation`/
  `--min-hold`)と入力名由来の `--output` は単一リテラルで表せないため `null`(算出規則は `help` に記す。
  プリセット由来の値は `presets` 側に載る)。`--recognizer-model-id`/`--recognizer-model-revision`も
  同様に `null`(実際の既定値は vocal_analysis の既定内容認識モデルが持つ
  `model_id`/`model_revision` の組であり、revision の実質的な既定は id の指定有無に
  依存するため、単一リテラルで表せない。算出規則は `help` に記す)。プリセットに依らない共通既定を持つ
  `--vowel-gain`/`--intensity-curve`/`--silence-threshold` は、その既定(8.1)をリテラルで載せる。
- `help`: 人間向け説明(5.2 の各引数説明)。

`presets` は各要素 `{name, values}` の配列。`name` は 8.1 のプリセット名、`values` は公開引数名→その
プリセットが与える値のオブジェクト(プリセットが定義しない引数は載せない)。

### 12.3 構造化エラー

失敗は終了コード(11章)に加え、機械モードでは error イベントで「どのフィールド/パスが・なぜ」を返す。
非機械モードでも理由を標準エラーへ最低1行出す(トレースバックは出さない)。各経路の `code`/`field`/
`exit_code`:

| 事象 | `code` | `field` | `exit_code` |
|---|---|---|---|
| 入力ファイルが存在しない、または音声として読み込めない・破損(利用可能な復号経路で試みて失敗した) | `not_audio` | `"input"` | 1 |
| 未知オプション・型/範囲エラー・positional 欠落等(argparse 検出)。組み合わせ検証(5.2)も含む: `--recognizer-model-id` を指定せず `--recognizer-model-revision` だけを指定した場合、`--forced-aligner sofa-forcedalign` 選択時に `--sofa-python`/`--sofa-root`/`--sofa-checkpoint` のいずれかが欠落した場合 | `bad_argument` | argparse が示す引数名(オプションは長形式フラグ名、positional は `"input"`)。組み合わせ検証は対象引数の長形式フラグ名(SOFA必須検証は `--sofa-python`→`--sofa-root`→`--sofa-checkpoint` の順で最初に見つかった欠落1件のみ) | 2 |
| 出力先に既存ファイルがある・`--overwrite` 未指定(5.3) | `output_exists` | `"--output"` | 2 |
| 出力書き込み失敗、または `--keep-intermediate` 指定時の中間生成物書き込み失敗(権限・不正パス・ディスク等の I/O 失敗) | `write_failed` | `"--output"` または `"--keep-intermediate"`(+ `path`) | 3 |
| 標準の読み込みが対応しない形式で、フォールバック復号器(ffmpeg)が未検出のため復号を試みられない(vocal_analysis.md §3) | `decoder_missing` | `"input"` | 4 |
| 分離・認識のモデル取得/実行失敗 | `stage_failed`(+ `stage`) | `null` | 4 |
| 上記いずれにも当たらない想定外の内部エラー | `internal_error` | `null` | 1 |
| 協調的な中断(Ctrl-C 等) | `cancelled` | `null` | 130 |

- `stage_failed` は失敗ステージの安定 id(12.1 progress の `stage` と同じ語彙)を `stage` キーで併載し、
  原因(取得失敗・実行失敗の別と元例外の要旨)を `message` に載せる。
- `internal_error` は表の各分類に当たらない未捕捉例外の受け皿で、CLI 本体の全体をトップレベルで
  捕捉して畳む。`KeyboardInterrupt` は内部エラーでなく中断(`cancelled`/`130`、12.4)として手前で分岐する。
- 複数の検証失敗が同一終了コードへ集約される場合も、`code`/`field` でどの検証がなぜ失敗したかを区別できる
  ようにする(規約 §5)。

### 12.4 中断

長時間処理(分離・認識・モーフ生成)を呼び出し側から安全に中断できる。

- **出力の原子性**: VMD 出力は一時ファイルへ書き切ってから最終パスへ置換する(`vmd.io`。5.3)。途中終了で
  中途半端な出力ファイルを残さない。診断用の中間生成物は song2vmd 自身が既定では追加で書き出さず(5.3)、
  `--keep-intermediate` 指定時のみ出力に隣接するディレクトリへ保存されるため、最終出力先を汚さない。
- **中断機構**: CLI 本体の全体で `KeyboardInterrupt`(端末 Ctrl-C / 親プロセスの中断)を捕捉する。
  **機械モード**では `cancelled` の error イベントを標準出力へ出してストリームを終端し、**非機械モード**では
  標準出力に JSON を出さず中断理由を標準エラーへ1行出す。どちらも終了コード `130` を返す。
- **Windows**: `CTRL_BREAK_EVENT` も上の `KeyboardInterrupt` 捕捉経路へ橋渡しし、`cancelled`/`130` として
  扱う。橋渡しの要否・実装は共有基盤([cli_events.md](../../libs/cli_events/cli_events.md) §5)が正。
- 呼び出し側がプロセスを強制終了した場合は終端イベントを出せないまま途切れる(規約 §4 の唯一の例外)。
  原子性により中途半端な出力は残らない。
