# lipsync 仕様書

各モーラの開き量を同梱した口形イベント列から、標準口モーフ(あ・い・う・え・お、必要に応じ閉口)による
**アニメ的(MMD的)な口パク**のモーフキーフレームを生成する共有ドメインモジュールの要求仕様。

---

## 1. 位置づけ

### 1.1 目的

複数のCLI(`vpr2vmd`・`song2vmd`)が共有する、アニメ的な口パクの**キーフレーム生成コア**を提供する。
入力(開き量を同梱した口形イベント列)の作り方はツールごとに異なる(音声認識＋RMS、または vpr 解析)が、
そこから先のキーフレーム生成ロジックは共通であり、これを `lipsync` に集約する。

口パクに必要な「時刻ごとの口形(母音)と開き量」を確定済みの抽象入力(2章)として受け取り、
3章の品質基準を満たす疎なモーフキーを出力する。入力をどう作るか(認識・分離・vpr解析・RMS算出)には
関与しない。

### 1.2 設計境界

CLAUDE.md の設計境界に従う**共有モジュール**(形式層でも単一ツール固有でもない第3カテゴリ)。

- VMD形式の入出力(モーフキーの書き出し)は `mmd_toolbox.vmd.io` に委譲する。`lipsync` はVMDの
  バイナリ構造を直接扱わない。モーフキーがキー間で線形評価される事実は `mmd_toolbox/vmd-interp.md`
  を正本とする(5章)。
- **標準口モーフ(あ・い・う・え・お)を使うという選択、母音の合成プロファイル、アニメ的口パクの
  生成判断は `lipsync`(ドメイン規約)に置く**。これは `mmd_toolbox` の契約ではない。
- **入力の作り方は `lipsync` の外**: RMS→開き量の写像、RMS・音量に基づく無音/閉口判定、音声認識・
  vpr 解析、特殊モーラの音響的確定などは各CLIの入口処理が担う。`lipsync` は確定済みの口形イベント列
  (各モーラの開き量を同梱)だけを受け取る(2章・6章)。
- 生成パラメータの**意味**(タイミング・誇張の各パラメータが何を制御するか)は `lipsync` が定める。
  その**具体値**(プリセット)とCLI引数は各ツール側に置く。`lipsync` は特定CLIに依存しない。

### 1.3 利用ツール

| ツール | 入力の作り方 |
|---|---|
| `vpr2vmd` | vpr の音符・音素・休符から口形イベント列を作り、vpr のベロシティ/ダイナミクスから開き量を決める |
| `song2vmd` | 音声認識＋RMSで口形イベント列を確定し、RMSから開き量を決める |

---

## 2. 入力契約

`lipsync` は次の抽象入力(§2.1 開き量を同梱した口形イベント列、§2.3 生成パラメータ)を受け取る。これらの
作り方(認識・vpr解析・RMS)はツールで異なり、`lipsync` は関与しない。各CLIはこの入力を**それぞれの入口で
用意して同じコアへ直接渡す**。共有するのはこのコアであって
特定の中間形式ではなく、あるCLIが別CLIの出力形式(例: vpr)を中間に挟む二段変換は前提としない(各CLIの変換経路は
独立に保ち、コア再利用と直経路の品質を両立する)。

**入力の共通契約(時間・列構造・開き量の同梱)**:

- **時刻はフレーム(30fps、float)**で与える(時間軸は5章のとおり30fps基準。秒→フレーム変換は呼び出し側)。
- 口形イベント列は **1イベント=1モーラ**。各イベントは開始フレームと終了フレームを持つ。
- イベントは時間順に並び、**隙間なく連続**し(各イベントの終端フレーム=次イベントの始端フレーム)、
  **重ならず全時間軸を被覆**する。
- 各イベントは §2.2 の開き量を **1つ同梱**する(モーラと開き量は1対1。別配列での並列受け渡しではなく
  イベントに同梱することで、配列間のインデックス取り違えを構造的に防ぐ)。

### 2.1 口形イベントタイムライン

口形イベントの列。各イベントは **種別・開始フレーム・終了フレーム・開き量(§2.2)** を持つ。種別は次の
いずれかで、モーラ単位の区切りを持つ。連続・非重複・全被覆の条件は上記「入力の共通契約」のとおり。

- **母音**: a/i/u/e/o のいずれか(→ あ/い/う/え/お モーフ)。
- **両唇閉鎖**: 一瞬しっかり口を閉じる区間(ま行・ば行・ぱ行に対応。閉鎖の判定は呼び出し側が済ませる)。
- **無音/休符**: 口を閉じる区間(全母音モーフ0付近)。

**無音/閉鎖の確定・母音の確定・境界の補正は呼び出し側(各CLI入口)の責務**であり、`lipsync` は確定済みの
イベント列を入力とする。連続する同一母音は観測上の一つの区間として渡してよい(`lipsync` は同母音連結で
扱う。4章)。

### 2.2 開き量(モーラ別の強弱)

各モーラ(=各口形イベント)の**口の開き量**。値域 **0〜1** で、合成全体のスケールに使う保持値。§2 の共通
契約のとおり各イベントに1つ同梱する。**母音イベントでのみ有意**で、無音・両唇閉鎖のイベントでは口を閉じる
ため開き量を使わない(既定 0)。連続同一母音を連結する際は、連結前の各イベントの開き量を保持区間内の強弱
節点として残す(4章)。各CLIが決める:

- `song2vmd`: ボーカルのRMS(声の強弱)から決める。
- `vpr2vmd`: vpr のベロシティ/ダイナミクスから決める(無ければ既定の開き量)。

RMS そのものや音声信号は `lipsync` に渡らない。`lipsync` が受け取るのは各モーラの開き量という確定値で
ある(RMS→開き量の写像規則・正規化は呼び出し側に残る。6章)。

### 2.3 生成パラメータ

キーフレーム生成の効かせ方を与えるパラメータ(タイミング・誇張)。意味は本仕様が定め(4章)、具体値は
呼び出し側(プリセット)が与える。

- 開き量の上限(開けすぎ防止)、母音別の開き量倍率。
- アタック/リリースのフレーム数、最小保持フレーム。
- 協調調音の重なり長(上限)、母音口形の先行フレーム数。
- 母音合成プロファイルの誇張係数、長い伸ばし音に許す揺らぎ量。

---

## 3. アニメ的口パクの定義(品質基準)

本モジュールが満たすべき口パクの質を定義する。生成方針(4章)はこれに従う。

- **設計された母音合成**: 各母音の口形を、標準口モーフ(あ・い・う・え・お、必要に応じ閉口)の
  重み付き合成として作る。素の母音モーフ1つを単独で立てるのではなく、母音ごとに自然に見える合成
  プロファイル(4章)を持つ。綺麗な口形のための混合は積極的に使う。ただし混合は「設計された合成」で
  あり、フレームごとに無制御に混ざるのとは異なる。
- **開きすぎない**: 口モーフを最大値(1.0)まで開かない。標準的な発声では中程度(目安0.5前後)を
  上限域とし、強い声で大きめ(目安0.8前後)、弱い声で控えめ(目安0.3前後)に収める。開きすぎは不自然。
  上限は開き量パラメータ(2.3)で与える。
- **読みやすい誇張**: 自然さの範囲で、横開き(い)や丸め(う・お)を実物よりやや大きめに強調し、
  アニメ的な分かりやすさを出す。
- **保持(プラトー)と最小保持**: 各母音は短いアタックで開き、モーラ区間の大半をほぼ一定値で保持し、
  短いリリースまたは協調調音の短い遷移で次へ移る。各母音区間の大半は目的口形を保持し、境界付近だけを
  短い遷移にすることで、線形補間によるだらだらした移り変わりを避ける。保持中はフレーム単位で揺らさない。
  保持が短すぎるモーラは間引き・併合して小刻みなパクつきを防ぐ。
- **協調調音(コアーティキュレーション)**: 隣り合う異なる母音の境界では短く重ねて移行し、自然な中間口形を
  作る。前の口形を境界付近まで保ってから短く切り替えるので、線形補間によるだらだらした移行にならない。遷移の
  長さは固定値だけでなく、前後の母音の口形差・区間長・間に両唇閉鎖があるかで決める。口形差が大きいほど短い
  中間口形を作り、短いモーラでは自動短縮する。両唇閉鎖以外の子音種別に応じた協調調音は将来対応とし、その際は
  口形イベント(2.1)に子音種別を加える。
- **同母音の連結**: 同じ母音が連続するときは、原則として一つの保持区間へ統合する。境界で再アタックや
  閉口を入れず、開き量差があるときだけ緩やかに保持値を変える。
- **口形の先行準備**: 母音の口形は、その音の開始より僅かに(最大1〜2フレーム)先行して作り始める。
  先行は直前が無音・休符のときに限りその末尾の短い準備窓へ入り、前区間長で自動短縮して無音の大半は
  侵食しない。両唇音では子音開始までに閉口を完成させ、解放後に母音口形へ移る(両唇閉鎖の直後は先行しない)。
- **子音と無音の口形**: 両唇閉鎖では一瞬しっかり口を閉じる。無音・休符の区間でも口を閉じる(直後に母音が
  続く無音・休符のみ、口形の先行準備で定める実効先行量の範囲で区間末尾に次母音が開き始める)。閉口は
  **全母音モーフを 0.0 にして表し、専用の閉口モーフは初期スコープでは設けない**(本章冒頭の「あ・い・う・え・お、
  必要に応じ閉口」の閉口は将来拡張の余地で、初期実装では使わない)。**明示的なモーフキーが無い区間はモーフ値 0.0
  とみなし閉口として扱う**ため、隣接母音のリリース/アタックの 0.0 キーと、母音に隣接しない区間のキー不在とで
  閉口は表現され、閉口専用のキー生成ステップは設けない。両唇閉鎖と無音はどちらも閉口だが、両唇閉鎖は子音開始
  までに閉口を完成させるタイミングを持つ点で扱いを区別する(口形の先行準備)。これらの区間は入力の口形イベント
  (2.1)として渡される。舌位置が主体の子音は標準母音モーフで無理に再現せず、前後母音の協調調音として扱う。
- **強弱は開き量で表す**: 声・音符の強弱は、各モーラの保持値(合成全体のスケール)を決める入力の開き量
  (2.2)で表す。フレーム単位の連続追従はしない。長く伸ばす音にだけ、ゆるやかな開閉の揺らぎを任意で
  許す。
- **疎なキー**: キーは要所(開き始め・保持・遷移直前・閉じ)に疎に置く。30fps前提でも全フレームに
  密に置かない。

この方針により、フレーム単位の追従に起因する小刻み・ぼやけを構造的に避ける。母音の単位はフレーム
ごとの推定ではなく、入力の口形イベント(から束ねたモーラ)に置く。

---

## 4. モーフ生成アルゴリズム

§3 の品質基準を満たすキーフレームを、入力(§2)から決定論的に生成する。母音合成プロファイルと各生成
パラメータの初期値は `lipsync` の既定として本章で確定し、誇張係数・各パラメータ(§2.3)で呼び出し側が
調整する。生成手順を次の各節で定める: 母音合成プロファイル(§4.1)、同母音連結(§4.2)、協調調音の遷移長
(§4.3)、最小保持・競合短縮(§4.4)、疎キー配置・量子化(§4.5)、任意の伸び表現(§4.6)、データ型とコア契約
(§4.7)、生成パラメータ一覧・初期目安(§4.8)、形状(§4.9)、先行準備(§4.10)、子音・無音の閉口規約(§4.11)。

### 4.1 母音合成プロファイル(初期重み)

各母音の口形を標準口モーフの重みの組として定義する(保持値1.0時の相対重み。行=目的母音、列=標準口モーフ)。

| 目的母音＼モーフ | あ | い | う | え | お |
|---|--:|--:|--:|--:|--:|
| あ | 1.0 | – | – | – | – |
| い | 0.1 | 1.0 | – | – | – |
| う | – | – | 1.0 | – | 0.2 |
| え | 0.2 | 0.2 | – | 1.0 | – |
| お | – | – | 0.2 | – | 1.0 |

表中の **–** は重み **0.0(寄与なし)** を表す(空欄・未設定ではない)。

- **主モーフと補助モーフ**: 各目的母音と同名の標準口モーフ(あ→"あ"・い→"い"・う→"う"・え→"え"・お→"お")を
  主モーフとし、同じプロファイル行で主モーフ以外に非ゼロ重みを持つモーフを補助モーフとする(表中 0.0 の
  モーフは寄与しない)。
- **合成値の算出順**(決定論):
  1. 目的母音のプロファイル行を選ぶ(表中 **–** は 0.0)。
  2. 補助モーフの重みに誇張係数 `exaggeration` を掛け(主モーフの重みには掛けない)有効プロファイルとする。
     誇張係数は呼び出し側のプリセットが与え、補助重みの増減でアニメ的な分かりやすさを調整する(既定 1.0)。
  3. 保持値 `hold = clamp(open_amount × vowel_scale[目的母音], 0.0, open_cap)` を求める(母音別倍率は
     呼び出し側が与える。既定 1:1:1:1:1)。
  4. 各モーフの暫定重みを `raw[m] = 有効プロファイル[m] × hold` とする。
  5. `sum(raw)` が `open_cap` を超える場合のみ、全モーフの暫定重みに `open_cap / sum(raw)` を掛けて
     比例縮小し、プロファイルの重み比を保ったまま合成後の総量を `open_cap` に収める(超えない場合は縮小しない)。
- **クランプは2段**: 保持値 `hold` の `open_cap` クランプ(手順3)と、合成後総量の比例縮小(手順5)のみとし、
  各モーフを個別に `open_cap` で頭打ちする処理は行わない。最終値は 0.0 未満を 0.0・1.0 超を 1.0 に防御的に
  丸めてよいが、既定値ではこの丸めに依存しない。
- **出力モーフ名・エンコード**: 標準口モーフは "あ"/"い"/"う"/"え"/"お" の5モーフ。出力は
  `mmd_toolbox.vmd.MorphKey`(`name_raw` は cp932・15バイト固定)。閉口は専用モーフを設けず全母音を 0.0 に
  落として表す(§3・§4)。プロファイル重みは `lipsync` の既定として持ち、`GenerationParams` とは
  別に定義する(誇張係数で補助重みのみ調整。差し替えは初期スコープ外)。

### 4.2 同母音連結

- 連続する同一母音区間を1つの保持区間へ連結する(再アタック・閉口を挟まない)。連結対象は時間順
  イベント列内で **shape が同一母音のまま連続する極大列**とする。両唇閉鎖・無音は母音合成プロファイルの
  対象外なので、同母音連結の境界として扱う。**1イベントだけの列は §4.9 の単一区間形状に一致**する。
- 同母音グループ `e_1..e_n`(時刻 `start_1` < 各内部境界 `b_i = end_i = start_(i+1)` < `end_n`)には、
  **先頭にのみアタック・末尾にのみリリース**を置き、内部境界には 0.0 キー・閉口キー・再アタックキーを
  置かない。保持領域を `hold_start = start_1 + attack_frames`、`hold_end = end_n - release_frames` とする。
- 連結後も、元の各小区間の開き量に対応する強弱節点を保持領域内に残し、節点間はモーフキーの線形補間
  ([vmd-interp.md](../mmd_toolbox/vmd-interp.md))で緩やかに保持値を変える。各モーフについて次のキーを置く:
  - **アタック**: `(start_1, 0.0)`・`(hold_start, w_1)`。**リリース**: `(hold_end, w_n)`・`(end_n, 0.0)`。
  - **強弱節点**: 各小区間 `e_i` の中央 `mid_i = (start_i + end_i) / 2` に、その小区間の保持値 `w_i` を置く。
    これにより各小区間は中央付近でその開き量を保ち、境界付近で次の開き量へ緩やかに遷移する。単一区間の
    グループは内部に開き量差が無く、強弱節点を別途置かずに §4.9 の単一区間形状へ帰着する。
- 各節点の保持値 `w_i` は §4.1 の合成手順で小区間ごとの `open_amount` から求める。同一グループ内では
  母音プロファイルは共通で開き量(保持値)だけが小区間ごとに変わるため、各モーフは同じ節点位置で重み
  `w_(i,m) = プロファイル_m × hold_i` を持つ。
- 本節は `hold_start ≤ mid_1 ≤ … ≤ mid_n ≤ hold_end` を満たす通常長のグループを対象とする。グループ長が
  短くこれを満たせない場合は本節で独自に短縮せず、§4.4 の最小保持・競合短縮で処理する。本節の時刻は
  フレーム単位の目標位置で、整数フレーム量子化・同一フレーム衝突・隣接キーのずらしは §4.5 でまとめて行う。

### 4.3 協調調音の遷移長

- 隣接母音の遷移長 = clamp( 基準長 ×(1 − 0.5・口形差), 1, min(重なり上限, 短い側区間長の1/2) ) を四捨五入で
  整数フレーム化する(基準長=重なり上限。初期目安は §4.8)。**口形差が大きいほど遷移を短くして素早く切り替える**
  (差ゼロ=同一母音は連結)。
- **口形差**(0〜1): 両母音の合成プロファイルの**正規化ユークリッド距離**。各母音の有効プロファイル(§4.1 の
  標準口モーフ重み。保持値1.0時の相対重みで、補助重みへの `exaggeration` 適用後・`hold`/`open_cap` による
  スケールは含めない)を あ・い・う・え・お の5次元ベクトルとし、各ベクトルを L2 正規化したうえでユークリッド
  距離を取り、それを `sqrt(2)` で割る。これにより同一口形は 0.0、互いに重ならない口形方向は 1.0 となる。
- **適用範囲**: 協調調音は、両唇閉鎖・無音を挟まずに直接隣接する(前母音グループの終端フレーム=次母音
  グループの始端フレーム)異なる母音グループ同士の内部境界にだけ適用する。同一母音の連続は §4.2 で連結済みの
  ため対象外。連続した有声区間の先頭は通常どおり 0.0 からアタックし、末尾は通常どおり 0.0 へリリースする。
  両唇閉鎖・無音を挟む境界(終端フレームが次の始端フレームと一致しない)では閉口を優先し、母音間遷移は作らない。
- **キー配置**: 境界フレーム `b`(前母音グループの終端=次母音グループの始端)と遷移長 `T` に対し、遷移区間を
  `b - T/2` から `b + T/2` までの中央配置とする。前母音の境界側保持点を `b - T/2`(重み `w_a[m]`)に、次母音の
  境界側保持点を `b + T/2`(重み `w_b[m]`)に置き、境界 `b` にあった閉口 0.0 キー(前母音のリリース終端・次母音の
  アタック始端)は置かない。各モーフについて前母音の保持重み `w_a[m]` から次母音の保持重み `w_b[m]` へ線形に
  遷移させ(片方のプロファイルに無いモーフは重み 0.0 とする)、境界 `b` には中間口形として
  `(w_a[m] + w_b[m]) / 2` のキーを置く。
- 間に両唇閉鎖があれば閉口を優先し、母音間遷移は作らない。両唇閉鎖以外の子音種別による微調整は将来対応
  (口形イベントに子音種別を加える。§3)。
- 本節は通常長の隣接母音を対象とする。遷移点がアタック・リリース・強弱節点と競合する短区間では本節で独自に
  位置を詰めず、§4.4 の最小保持・競合短縮で処理する。本節の時刻はフレーム単位の目標位置で、整数フレーム
  量子化・同一フレーム衝突(`T` が奇数で `b ± T/2` が同一フレームへ丸まる場合を含む)・隣接キーのずらしは
  §4.5 でまとめて行う。

### 4.4 最小保持・競合短縮

本節は §4.2 の母音グループ生成後・モーフキー出力前に実施する前処理ステップで、グループ正規化済みタイムラインと
各グループの実効タイミング値(フレーム単位の浮動小数)を出力する。整数フレーム量子化・同一フレーム衝突解消は
§4.5 が担い、本節は丸めを行わない。

- **最小保持の判定**: §4.2 の同母音グループ化後の各母音グループに適用する(連結前のイベント単位ではない)。
  グループ全長 `L = end − start`、最小保持を確保した残り `available = max(0, L − min_hold_frames)` とする。
  `available < 2`(最小保持に加えてアタック・リリース各最小1フレームすら置けない)のグループを短区間と判定し、
  下の併合/吸収で処理する。**最小保持は確保できるがアタック+リリース全量が収まらない**グループ
  (`2 ≤ available < attack_frames + release_frames`)は短区間とせず、下の競合短縮でアタック/リリースを詰めて残す
  (短区間=吸収対象 と 競合短縮=縮めて残す は排他で、`available` の 2 を境に分かれる)。

- **短区間の併合/吸収**: 短区間グループは次のいずれかで処理し、独立したモーフキーを残さない。
  - 直接隣接(間に無音・両唇閉鎖が無い)する同一母音グループがあれば §4.2 の連結へ倒す(`_vowel_groups` が
    既に同一母音連続を連結済みのため通常は防御的ルール)。
  - それ以外は隣接する生き残り母音グループへ時間 span を吸収する。吸収先は、短区間に面する隣接グループ境界
    イベントの開き量 `clamp(open_amount × vowel_scale[母音], 0, open_cap)` が大きい側を優先し、同値なら長い側、
    それも同値なら前側を選ぶ(決定論)。勝った隣接グループは境界側の保持区間を短区間 span ぶん延長してその span
    を貫き、短区間グループのアタック・保持・リリース・協調調音キーはすべて抑制する。両隣が母音で間に閉口
    イベントが残らない場合、吸収後に両隣接グループが直接隣接になる。両者が**異母音なら §4.3 の協調調音対象**、
    **同一母音なら §4.2 の同母音連結**として扱い、境界に再アタック・閉口を挟まず一保持区間へ統合する。
  - 吸収できる母音隣接が無い(両側が無音・時間軸端)短区間は除去し、その span は閉口(キーを出さない)とする。
  - **連続する短区間**: 異母音の短区間が直接隣接して続く場合は、極大な短区間 run を1単位として上の吸収を行う
    (単独の短区間は長さ1の run)。run の直前・直後に直接隣接する生き残り母音グループ(アンカー)の、run に
    面する境界イベントの開き量を上記タイブレーク(開き量大 → 長い側 → 前側)で比較し、run の span 全体を勝った
    アンカーへ吸収する(run 内の各短区間の独立キーはすべて抑制し、勝者がその span を貫いて保持/遷移する)。
    直接隣接するアンカーが片側のみならその側へ、両側とも母音アンカーが無ければ run を除去して閉口とする。
    これにより短区間が単独でも連続でも決定論的に前後へ吸収/併合される。

- **区間内の競合短縮**: 生き残った各グループについて、全長 `L`・最小保持 `m = min_hold_frames`・希望アタック
  `a = attack_frames`・希望リリース `r = release_frames` から実効アタック `a'`・実効リリース `r'` を求める:
  - `available = max(0, L − m)`(保持を最優先で確保した残り)。
  - `available ≥ a + r` なら `a' = a`、`r' = r`(縮小不要)。
  - `available < a + r` かつ `available ≥ 2` なら比例縮小する: `scale = available / (a + r)`、
    `a' = max(1, a × scale)`、`r' = available − a'`。さらに `r' < 1` なら `r' = 1`・`a' = available − 1` とし、
    アタック/リリースを各最小1フレームに保つ。
  - `available < 2` のグループは最小保持に加えてアタック・リリース各最小1フレームを置けない(`L < min_hold_frames + 2`)
    ため、上の短区間処理で吸収/除去済みでなければならない(最小保持 `m` 自体は `m ≤ L` の域で満たせる)。
  - 優先順位は **保持 > アタック/リリース > 先行・協調調音**。本節は協調調音・先行の新規規則を導入せず、短縮後の
    境界に対して §4.10 の先行クランプ・§4.3 の協調調音クランプ(隣接区間長の1/2上限)を適用するだけとする。
  - 求めた実効アタック `a'`・実効リリース `r'` は、そのグループの以降のキー配置すべてで `attack_frames`/
    `release_frames` の**代わりに**用いる: §4.9 のアタック到達 `start + a'`・リリース開始 `end − r'`、§4.2 の
    同母音連結の `hold_start = start_1 + a'`・`hold_end = end_n − r'`、§4.10 の先行準備のアタック到達
    `s − A_eff + a'`。これにより競合短縮した区間で保持優先が後段の連結・先行・協調調音に上書きされない。

本節は整数フレームの衝突に依存して小刻みを消さない(§4.5 より前に働く)。逆に、丸めだけが生む衝突は本節で
解決せず §4.5 に委ねる。

### 4.5 疎キー配置・量子化

- 各母音区間につき要所(開き始め・保持・遷移直前・閉じ)に疎にキーを置く。全フレームには置かない。本節は、
  §4.2/§4.3/§4.4/§4.9/§4.10/§4.11 が決めた要所キーを整数フレームへ確定する最終工程であり、フレーム単位の
  追従や汎用的な間引き(同一直線上の同値キー削除など)は行わない(疎さは生成側で既に満たす)。
- **30fps 整数フレームへの量子化**: キー生成中は `(モーフ名, 目標フレーム(float), 重み)` の目標値として保持し、
  最後に一括して整数フレームへ量子化する。丸めは Python の `round()`(銀行家丸め)ではなく `floor(x + 0.5)` の
  四捨五入(0.5 は切り上げ)とする。これにより `.5` フレーム(協調調音で遷移長 `T` が奇数のときの `b ± T/2` 等)の
  扱いがフレーム番号の偶奇に依存しない。協調調音の遷移長 `T`(§4.3)の整数化も同じ四捨五入に揃える。
- **同一モーフ・同一フレームの衝突統合**: 量子化後に同一モーフ・同一整数フレームへ複数の目標値が衝突した場合、
  量子化前の目標フレームが最も後ろの目標値を残す。量子化前の目標フレームも同一なら、生成手順上で後に決まった
  目標値を残す(=「最後に決まる目標値へ統合」の具体規則)。協調調音で `T = 1` となり `b − T/2`・`b`・`b + T/2` の
  一部または全部が同一整数フレームへ潰れる場合もこの規則で処理し、同一モーフ・同一フレームの重複キーは出力しない。
- **隣接キーの単調化**: 衝突統合後、モーフごとにキーを `(整数フレーム, 量子化前の目標フレーム, 生成順)` で昇順に
  並べて前から走査し、直前に確定した同一モーフのキーと同じフレームまたはそれ以前へ来るキーは、後続キー側を
  `直前フレーム + 1` へ進める。これにより同一モーフ内のキー列は厳密に昇順になる(ずらした結果さらに次と衝突する
  場合は同じ走査で順に後ろへずらす)。既に1フレーム以上離れているキーは動かさない(1フレーム間隔の離散遷移は
  正当なので保つ)。

### 4.6 任意の伸び表現

長く伸ばす母音にのみ、保持値を中心としたゆるやかな開閉の揺らぎ(伸び表現)を任意で加える。フレーム単位の
細かな追従はせず、揺らぎの極値にのみ疎にキーを置く(初期: 振幅 ±0.05・周期 約0.5秒。既定は控えめ)。
揺らぎ量は呼び出し側が `vibrato_*`(§4.8)で調整する。乱数を使わず、入力イベント列と `GenerationParams`
のみで決まる(§4.7)。

- **適用対象(保持プラトー)**: §4.4 で正規化した生き残り母音グループの実効保持プラトーを対象とする。
  プラトーの範囲はそのグループのエンベロープが保持値に達してから離れるまでで、端ごとに次で定める:
  - 協調調音しない端: 開始側は実効アタック終端(先行準備 §4.10 を含む立ち上がり完了点)、終了側は実効
    リリース始端。
  - 協調調音する端(§4.3): その境界の遷移窓の自グループ側の端。開始側は `境界 + T/2`、終了側は `境界 − T/2`。

  プラトー長 `plateau_len = plateau_end − plateau_start` が `vibrato_threshold` を超えるグループにのみ適用する。
  `vibrato_amp ≤ 0` または `vibrato_period ≤ 0` のときは適用しない(揺らぎ無効)。
- **公称開き量 `base_open(t)`**: プラトー上の時刻 `t` の公称保持開き量を、グループの保持・強弱節点
  (§4.2/§4.9)が表す開き量列の線形補間で定める。各イベント `j` の保持開き量を
  `hold_j = clamp(open_amount_j × vowel_scale, 0, open_cap)`(§4.1 と同じ)とし、制御点を
  「`(plateau_start, hold_先頭)`・各イベント中央 `(mid_j, hold_j)` のうちプラトー内のもの・`(plateau_end, hold_末尾)`」
  をフレーム昇順に取って線形補間する。単一イベントのグループでは一定値 `hold` になる。
- **揺らぎの適用**: プラトー上で公称開き量を正弦波で上下させ、その開き量から §4.1 と同じ合成手順で各口モーフ
  重みを求める(口形比率は保たれる)。時刻 `t` の実効開き量を
  `open_v(t) = clamp(base_open(t) + amp_eff(t) × sin(2π (t − plateau_start) / vibrato_period), 0, open_cap)`、
  `amp_eff(t) = min(vibrato_amp, base_open(t))`(静かな長音で揺らぎが公称開き量を超えて口が開き過ぎ・閉じ過ぎに
  ならないようにする)とする。`open_v(t)` から各モーフ重み = 有効プロファイル(§4.1 の誇張適用後)× `open_v`、
  合成総量が `open_cap` 超過なら比例縮小(§4.1 と同手順。個別モーフを `open_cap` で頭打ちしない)。
  `base_open(t) ≤ 0` の点には揺らぎを置かない。
- **節点(疎キー)**: 揺らぎはフレーム単位に追従せず、正弦波の極値にのみキーを置く。位相はプラトー始端を基準
  (`sin(0)=0`)に固定する。極値は周期 `P = vibrato_period` として `t_k = plateau_start + P × (1/4 + k/2)`
  (`k = 0, 1, 2, …`)に現れ、`plateau_start < t_k < plateau_end` を満たす厳密内側の点だけを節点とする(始端・
  終端と同時刻の点は出さない)。各 `t_k` で `sin` は +1, −1, +1, … と交替する。プラトー始端・終端の公称キー
  (保持値)はそのまま残し、値の連続性を保つ(線形補間のため傾きの不連続は許容)。
- **生成順と量子化**: 揺らぎ節点は公称エンベロープ・強弱節点・協調調音(§4.9/§4.2/§4.3)を生成した後に
  `(モーフ名, フレーム(float), 重み)` の float 目標として生成し、最後に §4.5 の一括量子化(四捨五入・同一
  フレーム衝突統合・単調化)へ渡す。極値は厳密内側にのみ置くので通常は公称キー・協調調音キーと衝突しないが、
  同一整数フレームへ潰れた場合は §4.5 の衝突統合に委ねる: 量子化前フレームが異なるなら後ろのものが残り、
  量子化前フレームも同一なら生成順が後(=揺らぎ節点)が残る。

### 4.7 データ型とコア契約

`lipsync` コアの公開データ型と関数契約をここで確定する(公開データ型の正本)。型は Python の dataclass /
Enum で表す。時刻はすべてフレーム(30fps、float)。

- **口形種別** `MouthShape`(列挙): `A` / `I` / `U` / `E` / `O`(母音 あ/い/う/え/お)、`BILABIAL`(両唇閉鎖)、
  `SILENCE`(無音・休符)。`BILABIAL` と `SILENCE` は出力上はどちらも閉口(全母音 0.0)だが、両唇閉鎖は
  閉口完成のタイミングを持つため生成上の扱いを区別する(§3・§4、4.3)。
- **口形イベント** `MouthEvent`: `shape: MouthShape`、`start: float`(開始フレーム)、`end: float`(終了フレーム)、
  `open_amount: float`(開き量 0〜1、母音区間のみ有意。既定 0.0)。1イベント=1モーラ(§2)。
- **イベント列の契約**: 時間順・隙間なく連続(終端=次の始端)・非重複・全時間軸被覆(§2)。
  これを破る入力は呼び出し側の不備とし、コアは前提として扱う。
- **生成パラメータ** `GenerationParams`: §4.8 の各フィールドを持つ平坦な dataclass。既定値は §4.8(初期目安)。
  具体値は呼び出し側プリセットが上書きする(§2.3。具体値の例は song2vmd 実装計画 §4.4。vpr2vmd は
  本既定を用い実データで調整する。vpr2vmd 実装計画 §5)。
- **コア関数**: `generate_morph_keys(events: Sequence[MouthEvent], params: GenerationParams) -> list[MorphKey]`。
  決定論的な純粋変換(乱数・外部依存なし)。入力時刻は float フレームで受け、出力 `MorphKey.frame`(int)へは
  §4.5 の規則で量子化する。返すのは時間順の `mmd_toolbox.vmd.MorphKey` 列(あ/い/う/え/お)。`VmdDocument` への
  組み立て・VMD書き出し(`mmd_toolbox.vmd.io`)・他セクション(ボーン等)は呼び出し側が行う(§5)。

### 4.8 生成パラメータの一覧・初期目安

`GenerationParams` の各フィールド。意味は §2.3 が正本で、ここで名前・単位・`lipsync` 既定(初期目安)を
確定する。既定は出発点で、各CLIプリセットが上書きする(具体値の例は song2vmd 実装計画 §4.4。vpr2vmd は本既定を
用い実データで調整する。vpr2vmd 実装計画 §5)。フレーム単位は30fps。

| フィールド | 意味(§2.3) | 単位 | 既定(初期目安) |
|---|---|---|--:|
| `open_cap` | 開き量の上限(開けすぎ防止) | 0〜1 | 0.8 |
| `vowel_scale` | 母音別の開き量倍率(a, i, u, e, o) | 倍率 | (1, 1, 1, 1, 1) |
| `attack_frames` | アタック(開き) | フレーム | 2 |
| `release_frames` | リリース(閉じ) | フレーム | 2 |
| `min_hold_frames` | 最小保持 | フレーム | 3 |
| `coartic_overlap_max` | 協調調音の重なり上限(=4.3 の基準長) | フレーム | 2 |
| `anticipation_frames` | 母音口形の先行準備(≤2) | フレーム | 1 |
| `exaggeration` | 母音合成プロファイルの誇張係数(補助重みへ乗算) | 倍率 | 1.0 |
| `vibrato_threshold` | 伸び表現を適用する保持プラトー長の下限(§4.6) | フレーム | 18(=0.6秒) |
| `vibrato_amp` | 伸び揺らぎの振幅 | 0〜1 | 0.05 |
| `vibrato_period` | 伸び揺らぎの周期 | フレーム | 15(=0.5秒) |

- `open_cap` は保持値クランプの上限であると同時に、§4.1 の合成後総量(誇張適用後)を比例縮小で収める上限でも
  ある。`lipsync` 既定は §3 の「強い声で 0.8 前後」に合わせた保守的な上限 **0.8**。各CLIは必要に
  応じ上書きする(song2vmd は `--open-max` で上書きし `pop` では 0.90)。適用順は §4.1。
- `open_cap` を除くタイミング・誇張の既定(アタック2/リリース2/重なり上限2/先行1/最小保持3/誇張1.0)は
  song2vmd の `pop` プリセット(同 §4.4)と整合させた出発点。`open_cap` だけは `lipsync` の保守的既定(0.8)で、
  song2vmd `pop`(0.90)とは独立に上書きされる。`vibrato_*` は 4.6 の初期値で、消費側が渡さない場合はこの既定が
  使われる。

### 4.9 形状(アタック・保持・リリース)

各母音区間の名目エンベロープ(形状の出発点)を定める。§4.1 で求めた各モーフの保持値 `w_m` に対し、単一の
孤立した母音区間 `[start, end]` につき、各出力モーフへ次の4点を置く:

1. `(start, 0.0)` — 区間開始は閉口 0.0。
2. `(start + attack_frames, w_m)` — アタックで保持値 `w_m` まで開く。
3. `(end - release_frames, w_m)` — 区間の大半を `w_m` で一定保持する。
4. `(end, 0.0)` — リリースで閉口 0.0 へ戻る。

これにより短いアタックで開き、保持区間を一定値に保ち、短いリリースで閉じる。保持中はフレーム単位で揺らさない
(揺らぎは §4.6 の伸び表現でのみ任意に加える)。

- **対象は通常長の区間**: 本節は `start + attack_frames < end - release_frames` を満たす区間を対象とする。
  アタックとリリースが競合する短区間(`start + attack_frames ≥ end - release_frames`)は本節で独自にクランプ
  せず、§4.4 の最小保持・競合短縮で処理する(前提を満たした区間が本節へ渡る)。
- **境界の扱いは前段の名目形状**: 孤立区間は閉口 0.0 から開き、閉口 0.0 へ閉じる。連続同一母音での境界の
  再アタック・閉口の回避は §4.2、隣接母音の協調調音による境界の中間口形は §4.3 が決め、本節はその前段の
  名目形状を与える。両唇閉鎖・無音区間は専用の閉口キーを置かず、隣接母音の 0.0 キーとキー不在で閉口を表す(§4.11)。
- **量子化は別管轄**: 本節の時刻はフレーム単位の目標位置として扱う。整数フレームへの量子化、同一モーフ・
  同一フレームのキー衝突解決、隣接キーが潰れた場合のずらしは §4.5 でまとめて行う。本節では同一フレームに
  重複する 0.0 キー(隣接区間の閉じと次区間の開き)をそのまま残してよい。

### 4.10 先行準備(anticipation)

§4.9 の母音グループ先頭アタックにのみ、母音口形の先行準備を適用する。母音口形を音の開始より僅かに先行して
作り始めることで、発音開始の時点で口形が立ち上がっている状態にする。対象は協調調音(§4.3)の入力境界を持た
ない母音グループ、すなわち直前の母音グループと直接隣接していない(coart-in でない)グループの先頭アタックに
限る。

母音グループ `g` の開始フレームを `s = g[0].start`、`g` の直前イベント(終端フレームが `s` に一致するイベント)
を `p` とし、先行量 `A_eff` を次で求める:

- 直前イベントが無い(時間軸の先頭)・直前が母音・直前が両唇閉鎖のいずれかなら `A_eff = 0`(通常アタック)。
  直前が母音の場合は §4.3 の協調調音が境界を担い、両唇閉鎖の場合は閉口完成を優先する(§4.11)ため、本節は適用しない。
- それ以外(直前が無音・休符)なら `A_eff = min(anticipation_frames, floor((p.end - p.start) / 2))`。
  `anticipation_frames` は §4.8 の先行フレーム数(≤2)。直前区間長の 1/2 を上限とすることで前区間を侵食しすぎない
  (自動短縮)。これにより `A_eff` は直前区間内に収まり、先頭アタックが負フレームへ出ることはない。

`A_eff = 0` のときは §4.9 の通常アタックをそのまま使う。`A_eff > 0` のときは、§4.9 の先頭アタックキー
`(s, 0.0)` と `(s + attack_frames, w_m)` を次へ置き換える(アタック長は変えず全体を `A_eff` フレーム前倒す):

- `(s - A_eff, 0.0)` ・ `(s - A_eff + attack_frames, w_m)`

短区間でのアタック・リリース・協調調音との競合短縮は §4.4、整数フレーム量子化・同一フレーム衝突は §4.5 が
担当する。

### 4.11 子音・無音の閉口規約

閉口は独立したキー生成ステップとしては実装しない。閉口は専用の閉口モーフではなく、標準口モーフ
「あ・い・う・え・お」すべての重み 0.0 で表す(§3)。両唇閉鎖・無音/休符の区間の閉口は次で
表現され、本節は新たなキーを追加しない:

- 母音に隣接する両唇閉鎖・無音区間は、§4.9 の母音リリース終端 `(end, 0.0)` キーまたはアタック始端
  `(start, 0.0)` キーで境界が閉じる(これらの区間は §4.2 のグループ境界として母音グループから除外され、
  自身のキーは出さない)。
- 直後に母音が続く無音/休符では、§4.10 の実効先行量 `A_eff` の範囲に限り区間末尾で次母音のアタックが
  始まる(無音の大半は閉口のまま)。
- 母音に隣接しない両唇閉鎖・無音区間、または閉口イベントだけで構成される区間には、明示的な 0.0 キーを
  追加しない。明示的なモーフキーが無い区間はモーフ値 0.0 とみなす(§3 の閉口規約)ため、キー不在が
  閉口を表す。これは §4.5 の疎キー配置(全フレームに置かない)とも一貫する。

`lipsync` 内で音量判定はしない。閉口イベントの確定は呼び出し側の責務(§6)。

---

## 5. mmd_toolbox との関係

- VMDの書き出しは `mmd_toolbox.vmd.io` に委譲する。生成するのはモーフキーのみで、ボーン・カメラ・照明・
  セルフ影セクションは空とする(最終的なVMD組み立て・出力は呼び出し側CLIが行う場合を含め、形式I/Oは
  `mmd_toolbox` を通す)。
- モーフキーは補間曲線を持たず、キー間は線形で評価される。この事実の正本は `mmd_toolbox/vmd-interp.md`
  とし、本仕様では重複定義しない。
- アニメ的な疎なキーは生成段階(4章)で直接置く。汎用キーフレーム疎化器は使わず、`mmd_toolbox` に
  モーフ用の疎化機能も求めない(設計境界どおり、アニメ的口パクの生成判断は `lipsync` 側、フォーマット
  入出力は `mmd_toolbox` 側)。
- 時間軸は30fps基準(`mmd_toolbox` 規約)に従う。

---

## 6. 含まないもの(呼び出し側の責務)

次は `lipsync` の責務ではなく、各CLIの入口処理に残る。

- **開き量の決定**: RMS→開き量の写像、声・音符の強弱の正規化(`song2vmd` は RMS、`vpr2vmd` は vpr 強弱)。
  `lipsync` は決定済みの開き量(2.2)を受け取る。
- **無音/閉口・母音・境界の確定**: 音量(RMS)に基づく無音/閉口判定、gap の無音/継続判定、両唇閉鎖の
  判定、母音の確定、境界の補正。`lipsync` は確定済みの口形イベント列(2.1)を受け取る。
- **入力の生成**: 音声の分離・認識、vpr の解析。
- **プリセットの具体値・CLI引数**: 生成パラメータ(2.3)の値は各CLIがプリセットとして与える。

---

## 7. テスト・回帰方針

`mmd_toolbox` のテスト規約(pytest・決定論・乱数シード固定・ネットワーク/GPU 不要)に従う。

- **共有コアの回帰はツール間比較では捉えられない**: `song2vmd` と `vpr2vmd` は同じ `lipsync` コアを通る
  ため、両者の出力を突き合わせても共有コアの不具合・回帰は相殺されて見えない。コアの正しさ・回帰は
  **既知値フィクスチャ**で別途担保する(ツール間比較では代替しない)。
- 入力(開き量を同梱した口形イベント列・生成パラメータ。§4.7)→モーフキー列 の決定論的変換を、合成した
  入力フィクスチャと手書きの**既知の期待キー値**(モーフ名・フレーム・ウェイト)で検証する。出力VMDは
  ラウンドトリップでも確認する。
- 各アルゴリズム要素(プロファイル合成、協調調音の遷移長、同母音連結、最小保持の併合/間引き、量子化の
  衝突解決、伸び表現)を、それぞれ独立した決定論的ユニットテストで検証する。
- テストデータはファイルとして持たず、テストコード内のフィクスチャとして組み立てる(MMD産ファイルは
  不要。`lipsync` コアは音声・vpr を介さないため)。

