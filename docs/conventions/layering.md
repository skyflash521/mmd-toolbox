# 層タクソノミーと配置規約

リポジトリのモジュールを 3 つの層に分類し、各層の責務・判定基準・物理配置・設計原則・依存方向を定める。
本書は**層の定義(抽象)を正本**とし、どのモジュールがどの層に属するかの具体は各モジュールの
仕様書(冒頭の層宣言)とリポジトリ構成(README)が示す。モジュールの追加・削除で本書を更新せずに
済むよう、本書では個別モジュール名を列挙しない。

## 1. 層タクソノミー

| 層 | 責務 | 判定基準(この層に置くもの) |
|---|---|---|
| フォーマット層 | 単一ファイル形式の読み書きと、その形式に閉じた評価・変換 | 特定のファイル形式の入出力と形式に閉じた処理だけを担い、ツール固有の判断を持たないもの。1 形式 = 1 ライブラリ |
| 共有ドメイン層 | 複数ツールが共有するドメインロジック | 形式層でも単一ツール固有でもないドメイン処理。形式の入出力はフォーマット層へ委譲するもの |
| CLIツール層 | CLI 引数・プリセット・そのツール限定のポリシー | エンドユーザー向けコマンド。形式の入出力・共有ドメインロジックは下位層へ委譲するもの |

- **フォーマット層**: 1 形式 = 1 ライブラリとし、各ファイル形式の入出力をそれぞれ独立したライブラリが
  担う(MMD 形式か否かを問わず同じ規律)。形式の事実だけを扱い、ツール固有の判断を持ち込まない([§3](#3-フォーマット層共通の設計原則))。
- **共有ドメイン層**: 複数 CLI が再利用するドメインロジックを、形式層でも単一ツール固有でもないため
  独立して置く。形式の入出力はフォーマット層へ委譲する。複数 CLI が共有する **CLI 出力契約(機械モードの
  イベント送出)** も、特定形式の入出力でも単一ツール固有でもないため、この層に含める。
- **CLIツール層**: そのツール固有の判断(CLI 引数、プリセット、ツール限定のポリシー)を持ち、
  形式の入出力・共有ドメインロジックは下位層へ委譲する。

新しい仕様や挙動を追加・変更する場合は、対応する正本ドキュメントを更新してから実装・テストと整合させる。

## 2. 物理配置

| ディレクトリ | 配置するもの |
|---|---|
| `libs/` | ライブラリ層(フォーマット層・共有ドメイン層の各モジュール) |
| `tools/` | CLIツール層(各 CLI のコマンド本体・仕様・テスト) |
| `docs/` | どの層にも属さない参照資料・規約(形式仕様 `docs/specs/`、プロジェクト規約 `docs/conventions/`) |

- 各モジュールは自身のディレクトリ直下に仕様書を置き、冒頭で自身の属する層を宣言する。
  どのモジュールがどの層に属するかは各仕様書の層宣言とリポジトリ構成(README)が示し、本書では持たない。
- バイナリレイアウト・形式レイアウトの正本は `docs/specs/` 配下に置き、各仕様書はそこを参照して
  重複記載しない。

## 3. フォーマット層共通の設計原則

フォーマット層の各モジュールは次の原則に等しく従う。各フォーマット層の仕様書は本節を参照し、
これらの原則を重複記述しない。

- **形式の事実のみを扱う**: ファイル構造の読み書きと、その形式に閉じた評価・変換だけを行う。ツール固有の
  判断(検出閾値、プリセット、生成・推定、値の選別・正規化など)は各ツールや共有ドメインの領分であり、
  持ち込まない。
- **ロスレス原則**: 解釈しないデータ(未解釈のバイト列・末尾の残バイト・未マップの形式エントリなど)も
  保持し、無加工なら入力とバイト一致で書き戻せる(各形式の保持範囲・往復保証は当該形式の仕様書で定める)。
- **CLI非依存・無出力**: 警告は構造化オブジェクトとして返し、表示の判断は呼び出し側(各ツール)へ委ねる。
  `print` や `logging` へ直接出力しない。
- **構造化警告・構造化エラー**: エラーにせず続行できる事象は構造化された警告のリストで報告し、正しい
  データモデルを構築できない構造異常は専用の構造化エラー(例外)で返す。両者を区別して扱う。

## 4. 依存方向と循環依存の禁止

循環依存は保守性を損なうため原則禁止する。依存を**構造依存**(コードの import・モジュール間の責務依存)と
**参照依存**(ドキュメント間のリンク・前提)の 2 軸に分け、構造依存は例外なく、参照依存は許可リストで例外を限定する。
コードとドキュメントは互いに参照させない([§4.3](#43-コードとドキュメントの間--相互に参照しない))。

### 4.1 構造依存(コード・モジュール) — 循環は例外なく禁止

- モジュール(`libs/<m>`・`tools/<t>` 単位。モジュール内のファイル分割は対象外)間の依存を循環させない。層内・層をまたぐを問わず A→…→A を作らない。
- 層をまたぐ依存は**下向きのみ**: CLIツール層 → 共有ドメイン層 → フォーマット層。下位層から上位層への上向き依存は禁止する(循環を生みやすい)。
- 同層モジュール間の一方向依存は循環ではなく本節では禁じない(各層の自己完結の度合いは [§1](#1-層タクソノミー) が定める)。
- 循環が必要に見える場合は共有概念を下位層へ抽出して解消し、容認しない。許可リストは設けない。

### 4.2 参照依存(ドキュメント間) — 許可リスト方式

可読性目的のリンクは片側だけ張り、循環にしない。本節はモジュール間の参照を対象とし、同一モジュール内で機能別に分割した仕様書どうしの相互リンク(主仕様書とその分割文書)は [§4.1](#41-構造依存コードモジュール--循環は例外なく禁止) と同様に対象外とする。下記のいずれかに該当し必須条件をすべて満たす相互参照だけを許可する。

| ID | 許可する循環 | 条件 |
|---|---|---|
| D1 | スコープの異なる正本同士の相互参照 | 各文書が別スコープの正本であり(例: 用語の正本と層の正本)、相手スコープは参照のみで定義を複製しない |
| D2 | 利用者向け README 間の相互参照 | リポジトリ全体の README と各ツールの README など利用導線に限り、内部仕様書(`docs/specs/`・各モジュール仕様書)へは広げない |

**必須条件(該当しても全て満たすこと):**

1. **工程循環の禁止**: 許可するのは内容の参照(リンク)のみ。「A が確定しないと B を書けない」承認・確定の循環は禁止する。
2. **定義の非複製**: 参照先の内容を複製しない(複製は二重管理として別途禁止)。

### 4.3 コードとドキュメントの間 — 相互に参照しない

コードが主・ドキュメントは従(主従の定義と適用範囲——実装の詳細に限る——の正本は
[document-authoring.md](document-authoring.md))であり、両者は互いを指さず、それぞれ単体で読める
ように自己完結で書く。

- **コードからドキュメントを参照しない。** コメント・docstring・出力メッセージ・テストコードを
  含め、説明のために仕様書・規約などのドキュメントを指ささない。従であるドキュメントの改名・分割・
  削除が主であるコードの記述を宙に浮かせる逆向き依存になるため。
- **ドキュメントからコードを参照しない。** ソースファイルのパス・`file:line`・関数/クラス名などの
  コード内部の識別子を指して説明を代替しない(「詳細は◯◯.py を参照」も含む)。主であるコードは
  自由に改修されるものであり、指した所在・名前が動くとドキュメントが陳腐化するため。ドキュメントは
  挙動・契約を自己完結で述べる。
- **対象外**: 仕様書・規約が自ら定義する外部契約の名前(CLI フラグ、機械モードの stage id、
  ライブラリの公開 API 名など)と、配置規約が定めるディレクトリ構成は、コードへの参照ではなく
  契約・規約の規定そのもの。ドキュメントファイルを処理対象のデータとして読み書きするコード
  (リリーススクリプトが `CHANGELOG.md` を抽出する等)のパス指定も、説明のための参照ではない。
  また、コード値の編集そのものを指示する調整手順書が編集対象を指す記述も、説明の代替ではなく
  作業対象の指定であり、対象外とする。
